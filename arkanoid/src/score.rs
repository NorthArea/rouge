/// Счёт игрока. Отдельный тип, а не голое число в игре: счёт живёт весь матч, переживает уровни и
/// показывается на экране, поэтому у него есть своё место.
#[derive(Default)]
pub struct Score {
    pub points: u32,
}
