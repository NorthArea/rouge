# Открытый backlog

В этом файле находятся только неначатые работы: scope, приоритет, зависимости, порядок и ожидаемая
проверка. Текущий статус и результаты выполнения записываются только в `workflow/progress.md`.

Очередь реализует вторую учебную игру репозитория — **Game 02 — Arkanoid** — и продолжается двумя
позициями общего меню выбора игры (`T-MENU-1`, `T-MENU-2`), принятого владельцем 2026-08-13 вместе с
решениями D-22 … D-24. Идея Arkanoid принята владельцем 2026-08-13 вместе с решением делать Arkanoid
в этом же репозитории; прежний запрет на вторую игру (D-04) и отсрочка идеи (D-04a) отменены тем же
решением. Ограничения, обязательные для всех позиций (стек, запрет лишних архитектурных конструкций,
структура репозитория, набор проверок качества, покрытие тестами, границы первой версии), записаны
решениями D-01 … D-24 в `workflow/roadmap.md` и здесь не повторяются. Полный чек-лист приёмки первой
версии Arkanoid — в описании позиции `T-ARK-12`; каждая позиция Arkanoid называет номера пунктов,
которые закрывает.

Зависимость всей очереди: Game 01 — Pong завершён и принят владельцем, принятое состояние помечено
тегом `pong-1.0`. Она выполнена до создания очереди.

## Очередь

Берите первую позицию, зависимости которой выполнены. Проверяйте зависимости в репозитории, а не
доверяйте таблице без проверки.

| Position | IDs | Scope | Depends on | Gate |
|---|---|---|---|---|
| 1 | `T-ARK-9` | Бонус `Wide Paddle`: детерминированное выпадение, падение, поимка ракеткой, временное расширение, снятие эффекта. | — | — |
| 2 | `T-ARK-10` | UI: `Score`, `Lives`, `Level` на экране и сообщения состояний `Press SPACE to start`, `LEVEL COMPLETED`, `GAME OVER`, `YOU WIN`. | — | — |
| 3 | `T-ARK-11` | `arkanoid/README.md` по одиннадцати пунктам принятой идеи и обновление корневого указателя. | `T-ARK-10` | — |
| 4 | `T-ARK-12` | Аудит покрытия по D-18, аудит простоты, полный набор проверок качества и подтверждение чек-листа приёмки первой версии. | `T-ARK-9`, `T-ARK-11` | Специальный: наблюдаемый прогон `cargo run -p arkanoid` по чек-листу первой версии, включая потерю жизни, бонус, `GameOver` и `GameWon` — подтверждает владелец. |
| 5 | `T-MENU-1` | Обе игры получают точку входа `pub async fn run()`: тело `main.rs` переезжает в `lib.rs`, `main.rs` худеет до вызова, формулировка `Esc` в README приводится к D-23. Игровая логика, тесты и отдельный запуск не меняются. | `T-ARK-12` | Специальный: наблюдаемый прогон `cargo run -p pong` и `cargo run -p arkanoid` — обе игры работают как принято. |
| 6 | `T-MENU-2` | Крейт `menu`: список игр, курсор, выбор, вызов `run()`, возврат в меню, выход из программы; запуск одной командой; README. | `T-MENU-1` | Специальный: наблюдаемый прогон — Pong → меню → Arkanoid → меню → выход, программа не закрывается ни разу до последнего `Esc`. |

Репозиторий уже переведён в cargo-workspace, Pong живёт в крейте `pong`, а крейт `arkanoid` с окном,
полем, ракеткой игрока, мячом, отскоком от ракетки, коллекцией блоков трёх типов со счётом, тремя
жизнями, состояниями игры, тремя уровнями и полным рестартом создан и сдан, поэтому позиции 1 и 2 не
имеют незакрытых зависимостей.

`Gate` — это дополнительная проверка, которой нет в обычном `make check`. Обычный `make check` уже
прогоняет весь набор проверок качества D-06 (`cargo fmt --check`, clippy, `cargo test`, `cargo build`)
по всему workspace, поэтому специальный gate нужен только там, где требуется наблюдаемый прогон игры.
О специальном gate нужно сообщить при начале задачи и запускать его только после обычной проверки.
Если требуется решение владельца, агент ждёт его и записывает ответ.

## Правила очереди

- Каждая открытая задача есть в упорядоченной очереди.
- У каждого ID очереди есть подробное описание, если только позиция специально не объединяет несколько ID.
- Завершённые записи удаляются после закрытия задачи.
- Заблокированные задачи и решения владельца не маскируются под готовую работу.

## Описание позиций

### `T-ARK-9` — бонус `Wide Paddle`

**Цель.** Из уничтоженных блоков иногда выпадает падающий бонус; пойманный ракеткой, он временно
расширяет её, а по истечении времени эффект исчезает.

**Scope.**

- `Bonus` как данные: позиция, размер, постоянная скорость падения; падение считается через delta
  time (D-09). Одновременно на поле может быть несколько бонусов — коллекция, как у блоков.
- Выпадение детерминировано (D-19): бонус роняет каждый четвёртый уничтоженный разрушаемый блок.
  Счётчик живёт в `Game` и сбрасывается при полном рестарте и при переходе на следующий уровень.
- Поимка: AABB-пересечение бонуса с ракеткой (D-12) увеличивает ширину ракетки на заданное время;
  по истечении времени ширина возвращается к исходной. Расширенная ракетка по-прежнему не выходит
  за границы поля.
- Непойманный бонус удаляется после выхода за нижнюю границу.
- Эффект и все падающие бонусы снимаются при потере жизни, переходе на уровень и полном рестарте.
- Единственный тип бонуса. Второго типа не появляется (D-21).

**Out Of Scope.** Любые другие бонусы, несколько мячей, визуальные эффекты (D-21).

**Что покрывается unit-тестом.** Этих пунктов нет в двенадцати обязательных пунктах D-18: принятая
идея бонус тестами не покрыла. Логика бонуса чистая и дешёвая в проверке, поэтому покрывается:

- каждый четвёртый уничтоженный разрушаемый блок роняет бонус, остальные три — нет;
- `Indestructible` счётчик выпадения не увеличивает;
- бонус падает пропорционально delta time;
- пересечение бонуса с ракеткой увеличивает её ширину и убирает бонус с поля;
- по истечении времени ширина ракетки возвращается к исходной;
- расширенная ракетка не выходит за границы поля;
- бонус, ушедший ниже нижней границы, удаляется и ширину не меняет;
- потеря жизни и полный рестарт снимают эффект, поле бонусов и счётчик.

**Проверка.** `make check`; специального gate нет. Фактическое выпадение и поимка подтверждаются
наблюдаемым прогоном в `T-ARK-12`.

**Пункты чек-листа приёмки.** 7.

### `T-ARK-10` — экранный UI и сообщения состояний

**Цель.** Во время игры на экране видны счёт, число жизней и номер уровня, а каждое состояние
показывает своё сообщение.

**Scope.**

- Постоянные надписи: `Score`, `Lives`, `Level` — примитивами Macroquad, без внешних шрифтов и
  assets (D-01).
- Сообщения по состояниям: `Press SPACE to start` (`WaitingToStart` и `LifeLost`),
  `LEVEL COMPLETED` (`LevelCompleted`), `GAME OVER` (`GameOver`), `YOU WIN` (`GameWon`).
- Текст латиницей: кириллические глифы встроенного шрифта Macroquad наблюдением не проверены, и в
  Pong по этой же причине подсказка написана латиницей.

**Out Of Scope.** Новые игровые механики, меню, экраны настроек, анимации (D-21).

**Что покрывается unit-тестом.** Сама отрисовка — ничем (D-10). Если выбор сообщения окажется
удобно вынести в чистую функцию «состояние → текст», она покрывается тестом соответствия каждого
состояния своему сообщению; если такой функции не появится, в карточке это записывается честно как
непокрытое рендерингом.

**Проверка.** `make check`; специального gate нет. Видимость надписей подтверждается наблюдаемым
прогоном в `T-ARK-12`.

**Пункты чек-листа приёмки.** 9.

### `T-ARK-11` — README Arkanoid

**Цель.** `arkanoid/README.md` объясняет игру и изучаемые в ней концепции программисту, который
знает программирование, но игры пишет впервые.

**Scope.** Одиннадцать пунктов принятой идеи:

1. Запуск — `cargo run -p arkanoid`.
2. Управление — таблица клавиш (`A`, `D`, `←`, `→`, `Space`, `R`, `Esc`).
3. Game loop и его стадии.
4. `delta time` и почему движение от него зависит.
5. Коллекция `Vec<Brick>`.
6. Жизненный цикл игровых объектов: блок уничтожается, бонус появляется и исчезает.
7. Collision detection: AABB, отскок от стен, ракетки и блока.
8. Игровые состояния и переходы между ними.
9. Уровни и их конфигурация в коде.
10. Бонусы.
11. Чем этот проект отличается от Pong и что нового в нём изучается.

Плюс обновление корневого `README.md` — указателя: Arkanoid из строки «в работе» становится
завершённой игрой со ссылкой в свой каталог.

**Out Of Scope.** Изменения кода игры, скриншоты и внешние ресурсы, описание каркаса работы с
агентом: он документирован в `.claude/README.md` и в README продуктов не упоминается (D-15).

**Что покрывается unit-тестом.** Ничего: это документация. Проверка — сверка с одиннадцатью пунктами
и с фактическим кодом (в README не должно быть описаний, которых нет в коде).

**Проверка.** `make check`. Специального gate нет.

**Пункты чек-листа приёмки.** 11.

### `T-ARK-12` — аудит и приёмка первой версии

**Цель.** Подтвердить, что первая версия Arkanoid соответствует принятой идее, а покрытие
unit-тестами соответствует D-18.

**Scope.**

- Аудит покрытия по двенадцати пунктам D-18 поимённо: для каждого пункта называется тест, который
  его закрывает. Недостающие тесты дописываются здесь; если пробелов нет, это фиксируется в карточке
  как результат аудита. Дописанный тест обязан сначала упасть по своей причине.
- Аудит простоты: запрещённых конструкций D-02 нет, требования D-07 выполнены, набор сущностей не
  вышел за D-17, ничего из списка «не входит в первую версию» (D-21) не просочилось, обязательных
  внешних assets нет, каждая игра читается целиком в своём крейте.
- Полный набор проверок качества D-06 по всему workspace и наблюдаемый прогон по чек-листу ниже.
- Проверка независимости скорости игры от FPS: на уровне логики — тестами эквивалентности шагов из
  `T-ARK-2` и `T-ARK-3`, на уровне игры — наблюдаемым прогоном.

**Out Of Scope.** Новые игровые возможности, рефакторинг ради красоты, любые расширения из D-21,
начало Game 03 — Asteroids.

**Что покрывается unit-тестом.** Только дописанные по результатам аудита тесты. Пункты 1, 2, 9, 10
и 13 чек-листа подтверждаются наблюдением и осмотром кода, а не тестом, и это записывается в карточке
честно.

**Проверка.** `make check` — он прогоняет весь обязательный набор D-06 по workspace, и каждая команда
должна дать код 0:

- `cargo fmt --all -- --check`;
- `cargo clippy --workspace --all-targets --all-features -- -D warnings`;
- `cargo test --workspace`;
- `cargo build --workspace`.

Затем специальный gate: наблюдаемый прогон `cargo run -p arkanoid` по чек-листу ниже, результат
подтверждает владелец.

**Чек-лист приёмки первой версии.** Работа считается завершённой, только если:

1. Одна управляемая ракетка — `T-ARK-2`, наблюдение здесь.
2. Один мяч, движущийся через delta time — `T-ARK-3`, наблюдение здесь.
3. Три типа блоков — `T-ARK-6`.
4. Счёт начисляется и отображается — `T-ARK-5`, `T-ARK-6`, `T-ARK-10`.
5. Три жизни — `T-ARK-7`.
6. Минимум три уровня — `T-ARK-8`.
7. Один тип бонуса `Wide Paddle` — `T-ARK-9`.
8. Состояния `GameOver` и `GameWon` — `T-ARK-7` и `T-ARK-8`.
9. Простой UI: `Score`, `Lives`, `Level` и сообщения состояний — `T-ARK-10`.
10. Unit-тесты игровой логики покрывают двенадцать пунктов D-18 — аудит здесь.
11. README объясняет изучаемые концепции — `T-ARK-11`, сверка здесь.
12. Проходят `cargo fmt --check`, clippy без предупреждений, `cargo test`, `cargo build` — здесь
    через `make check`.
13. Ручной прогон: игра пройдена минимум до второго уровня, проверены потеря жизни, получение
    бонуса, `GameOver` и `GameWon` — специальный gate, подтверждает владелец.

**Замечание к пункту 13.** Подтверждение `GameWon` требует пройти все три уровня подряд руками, и это
может оказаться долгим. Отладочных клавиш и сокращений ради gate не добавляется (это новая функция
вне scope). Если ручной путь до `GameWon` окажется непрактичным, решение принимает владелец: либо он
проходит игру целиком, либо принимает `GameWon` по unit-тесту `T-ARK-8` плюс наблюдение последнего
уровня, и его ответ записывается в строку реестра.

**После этой позиции очередь продолжается позициями меню `T-MENU-1` и `T-MENU-2`.** Меню стоит после
приёмки Arkanoid намеренно: до `T-ARK-12` выбирать в нём не из чего, а перевод Arkanoid на `lib.rs`
посреди его собственной очереди заставил бы все оставшиеся позиции `T-ARK-*` работать в новой
раскладке и добавил бы риск незаконченной игре. После приёмки это один чистый шаг, у которого в
качестве страховки есть готовый набор тестов обеих игр.

### `T-MENU-1` — точка входа у каждой игры

**Цель.** Каждую игру можно запустить и как самостоятельный бинарник, и вызовом из другого крейта;
принятый Pong при этом ведёт себя ровно как принятый.

**Scope.**

- `pong/src/lib.rs` и `arkanoid/src/lib.rs` с единственным публичным элементом `pub async fn run()`.
  Туда переезжает содержимое `main.rs`: цикл кадра, функции отрисовки, константы оформления, `Field`,
  чтение направления ввода.
- `main.rs` каждой игры оставляет только `window_conf` и
  `#[macroquad::main(window_conf)] async fn main() { <игра>::run().await }`. Игры остаются
  самостоятельными бинарниками: `cargo run -p pong` и `cargo run -p arkanoid` работают как прежде.
- Модули игры (`ball`, `game`, `paddle`, `score` у Pong) объявляются в `lib.rs` теми же строками
  `mod <имя>;` и остаются **приватными**. Публичной становится только `run()`. Это не чистоплюйство:
  при приватных модулях продолжает работать анализ `dead_code`, который в этом проекте уже ловил
  неиспользуемую константу под `-D warnings` (`T-PONG-5`); объявить модули `pub` — значит выключить
  эту защиту.
- `run()` не принимает аргументов и ничего не возвращает (D-02): размеры поля игра берёт из
  `screen_width()` / `screen_height()` сама, ровно как сейчас делает её `main`. Общего типа между
  играми не появляется — дублирующиеся `Field` остаются каждый в своём крейте.
- Строка про `Esc` в `pong/README.md:32` и соответствующая строка в README Arkanoid приводятся к
  D-23: `Esc` — возврат в меню; при отдельном запуске игры возвращаться некуда, и окно закрывается.

**Out Of Scope.** Сам крейт `menu` (`T-MENU-2`), любые изменения игровой логики, общие типы, общий
крейт или общие константы между играми (запрещено D-02), новые клавиши, смена заголовка окна.

**Что покрывается unit-тестом.** Ничего нового: это перенос кода, нового поведения нет. Наблюдаемый
RED по природе задачи недоступен, и в карточке это записывается честно — свидетельством служит сверка
состояния до и после.

**Защита принятого состояния Pong (тег `pong-1.0`).** Тег неизменен и служит эталоном; все три
проверки обязательны и записываются в карточку:

1. sha256 файлов `pong/src/ball.rs`, `pong/src/game.rs`, `pong/src/paddle.rs`, `pong/src/score.rs`
   снимаются до задачи и обязаны совпасть после неё побайтно: переезжает только `main.rs`, эти четыре
   файла не трогаются вовсе;
2. содержимое, переехавшее из `main.rs` в `lib.rs`, сверяется с `git show pong-1.0:src/main.rs` (в
   помеченном состоянии Pong жил в корне репозитория, поэтому путь в теге именно такой). Допускаются
   ровно три различия: сигнатура `pub async fn run()`, изъятый `window_conf` и изъятый атрибут
   `#[macroquad::main]`. Всё остальное — те же строки в том же порядке;
3. тестов Pong остаётся ровно 30 и с теми же именами, что сейчас. Это главный измеримый критерий
   того, что переезд ничего не сломал: все 30 живут в `ball.rs`, `game.rs`, `paddle.rs`, `score.rs`,
   а в `main.rs` тестов нет ни одного, поэтому ни одна проверка не переписывается и не удаляется —
   меняется лишь то, что они компилируются как тесты библиотеки, а не бинарника.

Если что-то разошлось, откат тривиален: переезд не меняет ни одной игровой строки.

**Зависимости.** `T-ARK-12`: обе игры должны быть приняты до того, как их точки входа меняются.

**Проверка.** `make check` (весь набор D-06 по workspace), затем специальный gate: наблюдаемый прогон
`cargo run -p pong` и `cargo run -p arkanoid` — обе игры работают как принято; подтверждает владелец.

### `T-MENU-2` — крейт меню

**Цель.** Одно приложение, одно окно, переключение между играми без выхода из программы.

**Scope.**

- Крейт `menu` добавляется в `members` корневого манифеста третьим участником workspace (D-22);
  `#[macroquad::main]` живёт в нём, и окно принадлежит ему.
- Чистая логика выбора — список пунктов, курсор, перемещение с заворотом, подтверждение выбора,
  намерение выйти — отделена от отрисовки и вызывается из тестов (D-10).
- Отрисовка списка примитивами Macroquad, без внешних шрифтов и assets (D-01), латиницей:
  кириллические глифы встроенного шрифта наблюдением не проверены, и в Pong подсказка написана
  латиницей по той же причине. Оформление — тот же минимализм, что в играх: тёмный фон, светлый
  текст, выделенный пункт отличается цветом.
- Пункты: `PONG`, `ARKANOID`, `QUIT`. `↑` / `↓` двигают курсор, `Enter` подтверждает выбор, `Space`
  делает то же самое (он уже означает «начать» в обеих играх), `Esc` в меню закрывает программу — так
  же, как пункт `QUIT` (D-23).
- `match` из трёх ветвей вызывает `pong::run().await`, `arkanoid::run().await` или завершает
  программу. Никакого trait'а, `Box<dyn Fn>` и таблицы игр (D-02).
- Меню не хранит игровых состояний и не знает игровых типов; после возврата из игры курсор остаётся
  там, где был, а повторный вход начинает игру заново (D-24).
- **После возврата из игры меню обязано сначала дождаться нового кадра и только потом читать ввод.**
  Обоснование — в разделе про наблюдаемую проверку ниже.
- Запуск одной командой без `-p`: сначала пробуется `default-members = ["menu"]` в корневом
  манифесте; если этот способ не даёт `cargo run` без `-p`, добавляется цель `make play`, и способ
  запуска называется в README.
- Корневой `README.md` и README обеих игр называют новую команду запуска, сохраняя
  `cargo run -p <игра>` как отдельный запуск для разработки.

**Out Of Scope.** Пауза внутри игры, экран настроек, громкость, выбор сложности; мышь, анимации и
эффекты выделения сложнее смены цвета, звук; таблица рекордов и сохранение результатов между
запусками; сохранение состояния игры между заходами в меню (отклонено владельцем, D-24); показ
управления игры в меню, скриншоты, превью; смена заголовка окна при входе в игру (способ сделать это
в Macroquad 0.4 не проверен); автоматическая сборка списка игр — три строки руками честнее и читаются
целиком; любые изменения игровой логики.

**Что покрывается unit-тестом.**

- перемещение курсора вниз и вверх по списку, включая заворот с последнего пункта на первый и обратно;
- подтверждение выбора даёт именно тот пункт, на котором стоял курсор;
- пункт `QUIT` даёт намерение «выйти»;
- `Esc` в меню даёт то же намерение «выйти», что и `QUIT`;
- соответствие пункта его подписи, если выбор подписи окажется чистой функцией; если такой функции не
  появится, в карточке это записывается честно как непокрытое рендерингом.

Не покрываются и покрыты быть не могут: отрисовка меню и привязка клавиш (D-10), сам вызов `run()` и
передача владения кадром (это Macroquad), сохранение позиции курсора после возврата и поведение `Esc`
в кадре возврата — всё это уходит в наблюдаемый gate.

**Ловушка кадра возврата — обязательный пункт наблюдаемой проверки.** `is_key_pressed` срабатывает по
факту нажатия в текущем кадре. Игра выходит из своего цикла, **не дождавшись** `next_frame()`, поэтому
управление возвращается в меню внутри того же самого кадра — и меню, прочитав ввод, увидит то же
нажатие `Esc` и немедленно закроет программу, то есть сделает ровно то, что владелец запретил.
Отсюда требование к реализации выше: после возврата из игры меню сначала дожидается нового кадра и
только потом читает ввод. Unit-тестом это не ловится принципиально — ввод в этом проекте не
тестируется (D-10), а сама ловушка живёт не в логике выбора, а в том, в каком кадре меню прочитало
клавиатуру; чистая функция выбора об этом ничего не знает и остаётся зелёной при сломанном поведении.
Единственная проверка — наблюдение: нажать `Esc` в игре и увидеть меню, а не закрывшуюся программу.

**Непроверенное, что подтверждается при реализации.** Ниже — гипотезы, а не установленные факты; ни
одну нельзя записать в карточку как проверенную без прогона в той же сессии:

- поведение `default-members` для `cargo run` без `-p` в виртуальном workspace не проверено; запасной
  вариант — цель `make play`, и выбор фиксируется по факту прогона;
- повторное чтение `Esc` меню в кадре возврата — гипотеза о поведении `is_key_pressed`, и именно
  поэтому она вынесена в пункт наблюдаемого gate, а не в тест;
- отсутствие остаточного глобального состояния Macroquad между двумя играми в одном процессе не
  наблюдалось. Обе игры используют только камеру по умолчанию, `draw_rectangle` и `draw_text`, и
  каждый кадр начинается с `clear_background`, поэтому утечка маловероятна — но это рассуждение, а не
  наблюдение. Проверяется тем же прогоном: вторая игра после первой выглядит и ведёт себя так же, как
  запущенная отдельно.

**Зависимости.** `T-MENU-1`: без точек входа у обеих игр меню звать нечего.

**Проверка.** `make check` — весь обязательный набор D-06 по workspace, каждая команда с кодом 0.
Затем специальный gate, результат подтверждает владелец:

1. одна команда запускает окно со списком `PONG`, `ARKANOID`, `QUIT`; стрелки двигают курсор;
2. `Enter` на `PONG` открывает Pong в том же окне, игра работает как принятая;
3. `Esc` в игре возвращает в меню и **не** закрывает программу (ловушка кадра возврата);
4. курсор после возврата стоит там же, где стоял;
5. `Enter` на `ARKANOID` открывает Arkanoid в том же окне, игра работает как принятая;
6. повторный вход в Pong начинает матч заново: счёт 0, объекты на исходных позициях, ожидание подачи
   (D-24);
7. `Esc` в меню (и пункт `QUIT`) закрывает программу; за весь прогон программа не закрылась ни разу
   до последнего `Esc`.

**После этой позиции очередь пуста.** Следующая игра — Game 03 — Asteroids: её нет в очереди, потому
что технического задания на неё ещё нет. Придумывать её задачи самостоятельно нельзя;
`NEXT_BACKLOG_ID` становится `none` до решения владельца.
