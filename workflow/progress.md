# Статус и ход выполнения задач

Текущий статус задач и указатель восстановления. Этот файл — единственный источник правды о статусе.

## Указатель восстановления

PROJECT_STATUS: IN_PROGRESS
CURRENT_TASK: none
NEXT_BACKLOG_ID: T-PONG-4
LAST_COMPLETED_TASK: T-PONG-3
LAST_GREEN_COMMAND: make check
LAST_CHECKPOINT: 8724c8c/T-PONG-2
NEXT_ACTION: Выполнить /workflow:build — developer берёт T-PONG-4 (столкновение мяча с обеими ракетками и отскок с углом, зависящим от места попадания); зависимости `T-PONG-2` и `T-PONG-3` выполнены в коде.

## Реестр статусов задач

Допустимые статусы строк: `IN_PROGRESS`, `DONE`, `BLOCKED`. Статуса `PENDING` нет: неначатые задачи
остаются в backlog.

| Task | Status | Scope / result | Verification |
|------|--------|----------------|--------------|
| T-PONG-1 | DONE | Cargo-проект Pong живёт в корне репозитория и открывает окно с полем: Macroquad подключён, game loop показывает стадии кадра явно, поле с рамкой и пунктирной центральной линией нарисовано, `Esc` закрывает игру. | RED: `make test` → exit 2, `manifest path .../Cargo.toml does not exist`. GREEN: `make test` → exit 0. Полный гейт: `make check` → exit 0. Unit-тестов нет по D-10 (рендеринг и ввод не тестируются). Специальный gate: прогон `cargo run` выполнял владелец, а не агент; gate подтверждён владельцем, дословный ответ — «ага, видно». |
| T-PONG-2 | DONE | Обе ракетки существуют как данные и движутся по вертикали через delta time с ограничением границами поля: логика ракетки вызывается из тестов без окна, `W`/`S` и `↑`/`↓` привязаны в стадии чтения ввода, обе ракетки нарисованы. | RED наблюдался по одному тесту за цикл, `make test-target TEST=<имя>` → exit 2 каждый раз: движение отсутствовало (`y = 250, ожидалось 450`); перемещение игнорировало направление (`секунда без ввода сдвинула ракетку: y = 450.00058`); не было верхней границы (`y = -149.99986, ожидалось 0`); не было нижней (`y = 649.99994, ожидалось 500`). GREEN: `make test` → exit 0 (4 passed). Точечно: `make test-target TEST=paddle` → exit 0. Полный гейт: `make check` → exit 0. Привязка клавиш и отрисовка unit-тестами не покрыты по D-10; фактическое управление подтверждается наблюдаемым прогоном в `T-PONG-8`. |
| T-PONG-3 | DONE | Мяч существует как данные и движется по вектору скорости через delta time, отражаясь от верхней и нижней границ поля с возвратом внутрь поля (без дребезга у границы); столкновение с границами занимает стадию проверки столкновений в кадре, мяч нарисован квадратом по D-12. | RED наблюдался по одному циклу за раз, `make test-target TEST=<имя>` → exit 2: движения не было (`шаг не сдвинул мяч по горизонтали: x = 100, ожидалось 250`); не было отражения от верхней границы (`velocity_y = -400, ожидалось 400`); не было от нижней (`velocity_y = 400, ожидалось -400`). Два теста позеленели с первого запуска и их RED получен мутацией: фиксированный шаг `1/60` вместо delta time дал `120 кадров по 1/120: мяч в (700, 399.9993), ожидалось (400, 300)`, а подмена условий границ на знак скорости дала `мяч внутри поля отразился: velocity_y = 100, ожидалось -100` и симметрично для нижней ветки; мутации откачены, sha256 файла совпал с исходным. GREEN: `make test` → exit 0 (9 passed). Точечно: `make test-target TEST=ball` → exit 0 (5 passed). Полный гейт: `make check` → exit 0 (первый прогон exit 2 на `cargo fmt --check`, форматирование исправлено). Отрисовка мяча unit-тестом не покрыта по D-10. |
