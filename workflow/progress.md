# Статус и ход выполнения задач

Текущий статус задач и указатель восстановления. Этот файл — единственный источник правды о статусе.

## Указатель восстановления

PROJECT_STATUS: IN_PROGRESS
CURRENT_TASK: none
NEXT_BACKLOG_ID: T-ROOM-12
LAST_COMPLETED_TASK: T-ROOM-11
LAST_GREEN_COMMAND: make check
LAST_CHECKPOINT: 3a4a6d4/T-ROOM-11
NEXT_ACTION: Выполнить /workflow:build — developer берёт T-ROOM-12 (аудит покрытия D-45, аудит простоты, полный гейт, чек-лист приёмки первой версии Game 05; последняя позиция очереди).

## Реестр статусов задач

Допустимые статусы строк: `IN_PROGRESS`, `DONE`, `BLOCKED`. Статуса `PENDING` нет: неначатые задачи
остаются в backlog.

| Task | Status | Scope / result | Verification |
|------|--------|----------------|--------------|
| T-ROOM-11 | DONE | `room/README.md` по пунктам задания: `Vec3` и третья ось (Y-up, пол и стены в плоскости XZ); мировые оси и transform (камера строится из `position`+`look_direction()` без отдельного типа/матриц, D-44); local/world coordinates — центральный раздел, почему `forward` игрока не мировая ось, `horizontal_axes` и порядок взгляд-раньше-движения (D-48); yaw/pitch и предел `PITCH_LIMIT` (89°, вырождение направления при строгой вертикали); перспективная камера и FOV (первая проекция в репозитории вместо прямого перевода координат); гравитация как вертикальная скорость с фиксированным порядком операций, прыжок через `on_ground`, а не таймер (D-47); collider — `Aabb`, строгое пересечение, `move_axis` по одной оси, скольжение как следствие порядка, вертикальная коллизия (приземление на куб / удар о нижнюю грань). Отдельные разделы: debug overlay, сравнительная таблица «чем Room отличается от предыдущих игр» (нет `GameState`, счёта, таймеров — D-47/D-43), таблица файлов. | RED/GREEN неприменимы: задача не добавляет поведения, набор тестов не изменился (266 тестов). Проверка — сверка каждого блока кода README с исходниками: `horizontal_axes`, `direction_from_angles`, `PITCH_LIMIT`, `jump`, `move_axis`, `intersects`, `apply_gravity` (сигнатура и первые строки), блок `set_camera(&Camera3D{...})` — все сверены дословно посимвольно (`diff` по извлечённым из исходника фрагментам без расхождений после исправления первой черновой версии — `move_axis`/`intersects`/`apply_gravity` изначально были пересказаны компактнее оригинала и приведены к точному виду). Упоминаний каркаса работы с агентом нет (D-15): `grep -in "workflow\|CLAUDE\|backlog\|agent" room/README.md` — без совпадений; хвостовых пробелов нет (`grep -n " $"` — без совпадений). Полный гейт: `make check` → exit 0 с первого прогона, 266 тестов без изменений — подтверждает, что задача не изменила поведение. Специального gate у позиции нет. |
| T-ROOM-10 | DONE | `room` — зависимость `menu/Cargo.toml`; `MenuItem::Room` — шестой вариант (`ITEMS: [MenuItem; 6]`, подпись `"ROOM"`, перед `QUIT`, после `Shooter`); `menu/src/main.rs` — ветка `MenuItem::Room => { room::run().await; next_frame().await; continue; }` с той же ловушкой кадра возврата D-23, что у четырёх предыдущих игр (снятие захвата курсора при выходе уже сделано в `room::run()` на `T-ROOM-3`/`T-ROOM-9`); корневой `README.md` — строка `Game 05 — 3D Room` со ссылкой на каталог и статусом «в разработке» (приёмка отложена вместе с Game 04, как у shooter), `cargo run -p room` добавлен в список запусков. | RED наблюдался компиляцией: `cargo test -p menu` → exit 101, «non-exhaustive patterns: `MenuItem::Room` not covered» сразу после добавления варианта в `enum`/`ITEMS`, в двух местах (`label()` и `main.rs` match), до правки `main.rs`. После добавления ветки в `main.rs` и `label()` — предсказанное падение существующего теста меню (прецедент `T-AST-11`/`T-TDS-13`): `quit_confirms_to_the_intent_to_quit` сломался бы на числе `move_down()` при шести пунктах — исправлено пятым `move_down()` до первого прогона (приведение к изменившемуся списку, не ослабление). Новый тест `moving_down_four_items_points_at_room` позеленел с первого прогона и закрыт изолирующей мутацией: перестановка `MenuItem::Room`/`MenuItem::Shooter` в `ITEMS` дала exit 101 «подтверждение после четырёх move_down вернуло Shooter, ожидалось Room»; мутация откачена, sha256 `selection.rs` (`1f0c906f…`) совпал до/после. Расширение `each_item_label_matches_its_name` строкой `Room.label()` — не новая проверка, не мутировалось отдельно. GREEN: `cargo test -p menu` → exit 0 (11 passed). Полный гейт: `make check` → exit 0 с первого прогона, 266 тестов (265 прежних + 1 menu). **Специальный gate (наблюдаемый прогон `cargo run`: меню → ROOM → комната → `Esc` → меню → повторный вход → комната заново → `Esc` → выход, программа не закрывается) пропущен по решению управляющего 2026-09-25 (default authority, прецедент Asteroids/Game 04); отложен до конца очереди Game 05, подтвердит владелец на `T-ROOM-12`.** |
| T-ROOM-9 | DONE | `lib.rs::draw_debug_overlay(&Player)` — три строки в левом верхнем углу (позиция x/y/z, yaw, pitch в градусах, 1 знак после запятой), рисуются после `set_default_camera` (экранные координаты, тот же порядок, что HUD в Shooter). Перевод радиан в градусы — `f32::to_degrees()` стандартной библиотеки, отдельной функции нет: заведение функции ради теста здесь было бы абстракцией ради тестируемости, запрещённой D-03 — решение названо явно в комментарии у функции и в этой строке реестра. | Тестов не добавлено: единственное поведение позиции — отрисовка, которая не тестируется (D-10), а форматирование сведено к одному вызову `format!`/`to_degrees()` без собственной логики, которую стоило бы выносить и тестировать отдельно. `cargo build --manifest-path ./Cargo.toml -p room` → exit 0. Полный гейт: `make check` → exit 0 с первого прогона, 265 тестов без изменений (229 прежних + 36 room, T-ROOM-8) — подтверждает, что позиция не изменила поведение, которое тестируется. Специального gate у позиции нет. |
| T-ROOM-8 | DONE | `Player::move_axis(delta, colliders)` — приватный примитив: сдвиг по одной оси, откат при пересечении с любым `Aabb`; скольжение — не отдельная функция, а следствие раздельного применения осей. `move_on_floor` (рефакторинг сигнатуры: добавлен параметр `colliders: &[Aabb]`, поведение с пустым списком не изменилось — все 6 старых тестов прошли без правки логики, только добавлен `&[]` в вызов) применяет X и Z раздельно через `move_axis`. `apply_gravity` (та же рефакторинг-добавка `colliders: &[Aabb]`) сохранил приземление на пол по `floor_level` без изменений (ultimate backstop — единственная поверхность на всю комнату, отдельного `Aabb` не заводится), и добавил вторую проверку — вертикальную коллизию со стенами/кубами: попадание сверху вниз (`delta_y < 0.0`) даёт то же самое, что пол (`on_ground = true`, `velocity_y = 0`), попадание снизу вверх гасит только скорость. `Room::colliders()` — стены+кубы одним `Vec<Aabb>` для `lib.rs::run()`. | Все 7 новых тестов написаны вместе с реализацией; 6 прошли с первого прогона (`cargo test -p room` → exit 0, 36 passed), один (`walking_diagonally_into_a_wall_slides_along_it`) прошёл с ошибочно слабым порогом (`x > 0.5`), не отличавшим скольжение от прилипания — усилен до `x > 5.0` (комментарий объясняет, почему порог именно такой) после того, как первая версия теста не поймала мутацию, объединяющую обе горизонтальные оси в один `move_axis`-вызов (наивное «столкнулся — отменить всё» из ловушки backlog). Семь изолирующих мутаций закрывают семь тестов: снятие отката `self.position -= delta` в `move_axis` поймала `walking_straight_into_a_wall_stops_before_it` (после усиления его же assertion с «не пересекается» на «по эту сторону стены» — первая версия не поймала мутацию: игрок при отсутствии отката проходил сквозь тонкую стену насквозь и переставал пересекаться с ней с другой стороны); `collided` захардкожен в `false` поймала `walking_into_an_obstacle_stops_before_it`; `collided` захардкожен в `true` поймала `walking_past_an_obstacle_is_not_blocked`; объединение X/Z в один вызов `move_axis` поймала усиленный `walking_diagonally_into_a_wall_slides_along_it`; снятие отката `position.y -= delta_y` в `apply_gravity` поймала `jumping_under_an_obstacle_kills_the_rise_and_does_not_pass_through`; снятие `velocity_y = 0.0` в том же блоке поймала `falling_onto_an_obstacle_top_stops_the_fall_and_sets_on_ground`; инверсия условия `delta_y < 0.0` → `delta_y > 0.0` поймала тот же тест на `on_ground`. `the_player_never_passes_through_any_of_the_four_room_walls` не мутировался отдельно — прогоняет тот же код `move_axis`/`move_on_floor` на все четыре направления через реальный `Room`, независимого пути отказа у него нет (тот же принцип, что у `gravity_returns_the_player_to_the_ground_after_a_jump` в `T-ROOM-6`). Все мутации откачены, sha256 `player.rs` (`d4345c7d…`) совпал до/после каждой (после `make format`). GREEN: `cargo test --manifest-path ./Cargo.toml -p room` → exit 0 (36 passed). Полный гейт: `make check` → exit 0 с первого прогона, 265 тестов (229 прежних + 36 room). Специального gate у позиции нет. |
| T-ROOM-7 | DONE | Новый модуль `aabb.rs`: `Aabb{min, max}` с конструктором `from_center_half_extents`, `Aabb::intersects` — три пары сравнений, строгие (соприкосновение гранью — «не пересекаются», решение зафиксировано тестом). `Room` получил `walls: Vec<Aabb>` (4 стены) и `obstacles: Vec<Aabb>` (3 куба) вместо разрозненных констант; `draw()` рисует из той же коллекции, что пойдёт в столкновения `T-ROOM-8` (нарисованное и просчитываемое больше не могут разойтись); неиспользуемое поле `wall_height` удалено (D-03: никаких неиспользуемых полей). `player::HALF_EXTENTS` (0.4, 0.9, 0.4) и `Player::aabb()` — коробка строится заново на каждый вызов из текущей позиции со смещением вниз на `EYE_HEIGHT - HALF_EXTENTS.y` (глаза не на самом верху тела). | Все 7 новых тестов (6 `aabb::` + 1 `player::`) написаны вместе с реализацией и прошли с первого прогона (`cargo test -p room` → exit 0, 29 passed) — закрыты семью изолирующими мутациями по одной за раз при exit 0 контрольных тестов: замена Z-пары на `true` поймала тест «перекрытие по X/Y, разнесение по Z»; замена X-пары поймала тест «разнесение по X»; замена Y-пары поймала тест «разнесение по Y»; `<`/`>` → `<=`/`>=` поймала тест касания гранью; `false && ...` поймала тест вложенных коробок; обе X-проверки → `true` (а не одна — одной было недостаточно, вторая X-проверка сама по себе уже разделяла эти коробки, что и показала первая попытка) поймала тест разнесённых коробок; `Player::aabb` без вертикального смещения (`center = self.position`) поймала тест «коробка строится из позиции и половинных размеров». Все мутации откачены, sha256 `aabb.rs` (`90d8aa0a…`) и `player.rs` (`26786217…`) совпали до/после каждой (после `make format`). GREEN: `cargo test --manifest-path ./Cargo.toml -p room` → exit 0 (29 passed). Полный гейт: `make check` → exit 0 с первого прогона, 258 тестов (229 прежних + 29 room). Специального gate у позиции нет. |
| T-ROOM-6 | DONE | `Input` получил `jump: bool` (`is_key_pressed`, не `is_key_down` — комментарий у поля называет ловушку удержания клавиши); `player::JUMP_IMPULSE` (8.0); `Player::jump(&Input)` — одна ветка: нажат прыжок и `on_ground` истинен → `velocity_y = JUMP_IMPULSE`, `on_ground = false`; запрет второго прыжка в воздухе держит признак «на земле», а не счётчик/таймер (D-47). `lib.rs::run()` вызывает `player.jump(&input)` перед `apply_gravity` в стадии 3. | Все 4 новых теста `player::` написаны вместе с реализацией и прошли с первого прогона (`cargo test -p room` → exit 0, 22 passed) — три закрыты изолирующими мутациями по одной за раз при exit 0 контрольных тестов: снятие `&& self.on_ground` поймала `jumping_in_the_air_does_not_change_the_vertical_velocity`; снятие присваивания `velocity_y = JUMP_IMPULSE` поймала `jumping_from_the_ground_makes_the_vertical_velocity_positive_and_clears_on_ground`; снятие `self.on_ground = false` внутри `jump()` поймала тот же тест `jumping_from_the_ground...` (при этой мутации `holding_jump_does_not_grant_a_second_jump_before_landing` не отличила исход — между двумя вызовами `jump()` в тесте стоит `apply_gravity`, который сам пересчитывает `on_ground` по позиции и маскирует утечку значения; названо честно, а не скрыто). Четвёртый тест, `gravity_returns_the_player_to_the_ground_after_a_jump`, не мутировался отдельно: это прямая композиция уже промутированных `jump()` и `apply_gravity()` (T-ROOM-5), собственного независимого пути отказа у него нет — тот же принцип, что у `the_rule_is_deterministic` в `T-TDS-10`. Все мутации откачены, sha256 `player.rs` (`ced3afd0…`) совпал до/после каждой (после `make format`). GREEN: `cargo test --manifest-path ./Cargo.toml -p room` → exit 0 (22 passed). Полный гейт: `make check` → exit 0 с первого прогона, 251 тест (229 прежних + 22 room). Специального gate у позиции нет. |
| T-ROOM-5 | DONE | `Player` получил `velocity_y: f32` и `on_ground: bool`; `player::EYE_HEIGHT` (1.6) — высота глаз, приземление ставит `position.y` на `floor_level + EYE_HEIGHT`, а не на сам пол; `Player::apply_gravity(gravity, delta_time, floor_level)` — фиксированный порядок «скорость → позиция → приземление» (комментарий объясняет визуальное дрожание при обратном порядке). `Room` получил поле `floor_level` (0.0), отрисовка пола/стен переведена на него вместо жёсткого нуля. `lib.rs::run()` вызывает `player.apply_gravity(GRAVITY, delta_time, room.floor_level)` в стадии 3 после движения по полу. | Все 6 новых тестов `player::` написаны вместе с реализацией и прошли с первого прогона (`cargo test -p room` → exit 0, 18 passed) — закрыты шестью изолирующими мутациями по одной за раз при exit 0 контрольных тестов: снятие вычитания гравитации из скорости поймала тест «падает и ускоряется»; замена накопления скорости (`-=`) на присваивание (`=`) поймала тест ускорения за два кадра; снятие снапа `position.y = eye_level_on_floor` поймала тест «останавливается ровно на уровне пола»; снятие обнуления `velocity_y` поймала тест приземления; снятие `on_ground = false` в ветке "в воздухе" поймала тест `being_airborne_clears_on_ground`; сдвиг уровня приземления на `+0.05` поймала тест «на полу без ввода не дрожит». Все мутации откачены, sha256 `player.rs` (`c13f21d6…`) совпал до/после каждой (после `make format`). GREEN: `cargo test --manifest-path ./Cargo.toml -p room` → exit 0 (18 passed). Полный гейт: `make check` → exit 0 с первого прогона, 247 тестов (229 прежних + 18 room). Специального gate у позиции нет. |
| T-ROOM-4 | DONE | `input.rs::Input` — данные одного кадра (mouse_delta + четыре клавиши движения, D-35/D-44); `player.rs::horizontal_axes(yaw)` — единственный в крейте переход "yaw → горизонтальное направление" (D-48), pitch сознательно не участвует; `Player::move_on_floor(&Input, speed, delta_time)` — вектор намерения из клавиш, нормализуется только если не нулевой (без NaN на противоположных клавишах), × скорость × delta time. `lib.rs::run()` строит `Input` из `is_key_down` W/A/S/D + `mouse_delta_position()`, вызывает `player.move_on_floor` в стадии 3. | Все 6 новых тестов `player::` написаны вместе с реализацией и прошли с первого прогона (`cargo test -p room` → exit 0, 12 passed) — закрыты шестью изолирующими мутациями по одной за раз при exit 0 контрольных тестов: знак `-yaw.cos()` → `yaw.cos()` в `forward` поймала тест на нулевой yaw; удвоение `forward`/`right` (по отдельности) поймало тест на единичную длину; инверсия знака `right` поймала тест на поворот 90°; замена `delta_time` на фиксированный шаг `0.01` поймала тест FPS-независимости (первая мутация — `min(1/60)` — оказалась неразличима при 120fps < 60fps, отброшена и заменена); безусловный `intent.normalize()` без ветки поймала тест диагонали (падение до отсутствия нормализации) и тест противоположных клавиш (NaN на нулевом векторе) по отдельности. Все мутации откачены, sha256 `player.rs` (`350c0fbb…`) совпал до/после каждой (после `make format`). GREEN: `cargo test --manifest-path ./Cargo.toml -p room` → exit 0 (12 passed). Полный гейт: `make check` → exit 0 с первого прогона, 241 тест (229 прежних + 12 room). Специального gate у позиции нет. |
| T-ROOM-3 | DONE | `player.rs`: `Player{position, yaw, pitch}` (D-44); `Player::look(mouse_delta, sensitivity)` — единая точка "мышь → взгляд": прибавляет к yaw, вычитает из pitch, pitch зажат `clamp_pitch` в пределе `PITCH_LIMIT` (±89°, вертикаль исключена намеренно), yaw приводится к `(-π, π]` функцией `wrap_angle` (решение зафиксировано тестом `yaw_wraps_instead_of_growing_without_bound`, а не оставлено случайным); `Player::look_direction()` — единственный переход "yaw/pitch → направление" (`direction_from_angles`, D-48), которым пользуется камера рендеринга как `target = position + look_direction()`. `lib.rs::run()`: `set_cursor_grab(true)`/`show_mouse(false)` при входе, снятие при выходе по `Esc`; камера следует за позицией и взглядом игрока вместо фиксированной точки `T-ROOM-2`. | RED неприменим впрямую (по прецеденту `T-TDS-8`/`T-TDS-10`): все 6 тестов `player::` написаны вместе с реализацией и прошли с первого прогона, `cargo test --manifest-path ./Cargo.toml -p room` → exit 0 (6 passed) — поэтому закрыты шестью изолирующими мутациями по одной за раз при exit 0 контрольных тестов: `pitch.clamp(...)` → `pitch` (без ограничения) поймала оба граничных теста; сдвиг `+ 0.5` внутри `clamp` (пределы остались, значение внутри — нет) поймала `pitch_within_the_limit_is_not_clamped`; `self.yaw + mouse_delta.x * sensitivity` → `mouse_delta.x * sensitivity` (без накопления) поймала `yaw_accumulates_from_successive_mouse_deltas`; `wrap_angle` заменена на identity поймала `yaw_wraps_instead_of_growing_without_bound`; перестановка `sin`/`cos` в `direction_from_angles` поймала тест на нулевые углы. Все шесть мутаций откачены, sha256 `player.rs` (`f1fff925…`) совпал до/после каждой (до `make format`). GREEN: `cargo test --manifest-path ./Cargo.toml -p room` → exit 0 (6 passed). Точечно: `make check` → exit 2 на первом прогоне (`cargo fmt --check` — многострочный `vec3(...)`), исправлено `make format`; второй прогон → exit 0. Полный гейт: `make check` → exit 0, 235 тестов (52 Arkanoid + 62 Asteroids + 10 menu + 30 Pong + 6 Room + 75 Shooter). API `mouse_delta_position`/`set_cursor_grab`/`show_mouse` выяснены `cargo doc -p macroquad --no-deps` в `target/doc`. Специального gate у позиции нет; mouse look подтверждается чек-листом `T-ROOM-12`. |
| T-ROOM-2 | DONE | Новый крейт `room` шестым участником workspace: `pub async fn run()` открывает окно, перспективная `Camera3D` (FOV 60° по вертикали) смотрит на комнату из пола, четырёх стен и трёх кубов-препятствий (модуль `room::Room`), стадии кадра D-09 видны как явные комментарии-заголовки; `Esc` выходит. Камера пока фиксирована в точке комнаты и смотрит в центр — игрока ещё нет (появится `T-ROOM-3`). | RED наблюдался по прецеденту (`T-AST-1`/`T-TDS-1`): `cargo test --manifest-path ./Cargo.toml -p room` → exit 101 «package ID specification `room` did not match any packages». GREEN: та же команда после создания крейта → exit 0 (0 тестов — позиция без тестируемого поведения, D-10, подтверждено backlog). API `Camera3D`/`draw_cube`/`draw_plane`/`draw_grid` выяснены компилятором и `cargo doc -p macroquad --no-deps` (сборка в `target/doc` внутри репозитория) — подтверждён Y-up (`draw_plane`/`draw_cube` кладут высоту в `y`, `draw_grid` строит сетку в плоскости XZ). Полный гейт: `make check` → exit 0 с первого прогона (229 тестов — набор не изменился, у позиции нет тестов). **Специальный gate (наблюдаемый прогон `cargo run -p room`) пропущен по решению управляющего 2026-09-25 (default authority, прецедент Asteroids/Game 04); отложен до конца очереди Game 05, подтвердит владелец на `T-ROOM-12`.** |
| T-TDS-15 | DONE | Аудит покрытия D-36 закрыл все 14 пунктов поимённо без пробелов (перечень в отчёте задачи: направление к курсору — `player::`, перевод координат и следование камеры — `camera::`, cooldown/lifetime пули — `bullet::`, circle-to-circle — `collision::`, урон/смерть/очки врага — `combat::`, урон игроку/cooldown — `game::`, pickup каждого вида — `pickup::`, волна/GameOver/restart — `game::`). Аудит простоты нашёл и исправил один реальный дефект — `unwrap()` без необходимости в `combat::resolve` (единственный `unwrap()` в продакшн-коде крейта, не встречавшийся ни в одной из трёх предыдущих игр) — заменён на `unwrap_or(false)`, рефакторинг без изменения поведения. Аудит нашёл два отклонения от буквального D-35/D-40, принятых как обоснованные (не исправлялись кодом — правка ушла бы в `roadmap.md`, чужую зону): `Input` — структура для группировки прочитанного за кадр ввода, нужна, чтобы `Game::update` вызывался из тестов без окна (D-10) без раздувания сигнатуры до восьми позиционных `bool`; `Effect::time_remaining` — пятый по счёту таймер сверх заявленных в D-40 четырёх, тот же паттерн визуального эффекта смерти, что уже был принят для Asteroids (D-29 там же). Остальной аудит — без замечаний: `grep` не находит `use pong`/`use arkanoid`/`use asteroids`, общего модуля, `trait`/`dyn`/`Box<dyn>`/generic-параметров, `panic!`/`static mut`/`lazy_static`/`.clone()`/загрузки внешних файлов; единственная зависимость — `macroquad`, без `rand` и AI/pathfinding-библиотек; `screen_to_world` вызывается из продакшн-кода ровно один раз (D-39). Размер: 2343 строки в `shooter/src/*.rs` с тестами, 75 тестов — внутри ожидаемых 2400–3000 строк / 70–90 тестов (чуть меньше по строкам, но в пределах порядка), порог тревоги ~3500 не достигнут. | Рефакторинг `unwrap()` → `unwrap_or(false)` — не новое поведение: `cargo test -p shooter --lib` → exit 0 (75 passed) до и после замены. GREEN: `cargo test -p shooter --lib` → exit 0 (75 passed); `cargo test --manifest-path ./Cargo.toml --workspace` → exit 0 (229 тестов: 52 Arkanoid + 62 Asteroids + 10 menu + 30 Pong + 75 Shooter). Полный гейт: `make check` → exit 0 с первого прогона (все четыре команды D-06). **Специальный gate (наблюдаемый прогон по чек-листу приёмки первой версии, 14 шагов) пропущен по решению управляющего 2026-09-25 (default authority, по прецеденту решения владельца для Asteroids); наблюдаемый прогон отложен до конца очереди Game 04 — эта позиция была последней.** Чек-лист приёмки (23 пункта) верен по коду и тестам для всего, что не требует экрана; шаги, которые остаются владельцу: запуск через `cargo run` из меню и переключение SHOOTER↔меню без закрытия программы (пункт 21); визуальное прицеливание мышью, вид ствола (частично пункт 3); визуальное следование камеры и остановка у края арены (частично пункт 12); отображение HUD (`Health`/`Ammo`/`Score`/`Wave`) и сообщений состояний на экране (пункт 20); прохождение минимум двух волн и получение `GameOver` вручную; полный список — из чек-листа задачи в карточке (уже удалена). |
| T-TDS-14 | DONE | `shooter/README.md` по прецеденту `asteroids/README.md`: запуск (меню и `cargo run -p shooter`), управление, что видно на экране, game loop, присвоенная скорость игрока (сравнение с инерцией Asteroids), камера и мировые/экранные координаты (отдельный крупный раздел — единственный перевод `screen_to_world`, почему камера стоит раньше мыши в очереди, ловушка UI при активной `Camera2D`), прицеливание мышью, стрельба и края арены вместо wrap, chase AI и детерминированная раскладка волны, единственная на крейт проверка круг-круг и четыре пары, которые ею пользуются, здоровье/контактный урон/неуязвимость с объяснением порядка tick-перед-проверкой, боеприпасы, pickups, волны, диаграмма состояний, сравнительная таблица «чем отличается от Asteroids», таблица файлов. | RED/GREEN неприменимы: задача не добавляет поведения, набор тестов не изменился (75 тестов). Проверка — сверка каждого утверждения README с исходниками: шесть блоков кода сверены дословно (`camera.rs::camera_center` — построчно идентичен с точностью до форматирования `clamp`; `player.rs::aim_at`, `bullet.rs::is_outside`, `collision.rs::circles_overlap` — побайтово; `game.rs` — строка `tick(&mut self.invulnerability_timer, delta_time); self.check_player_collision();` сверена точным совпадением; `player.rs::update` — сверен). Блок `set_camera(&Camera2D{...})` сверен с `lib.rs` построчно. Таблица управления — с чтением клавиш в `run()`; таблица файлов — с фактическим списком `shooter/src/*.rs` (11 модулей). Упоминаний каркаса работы с агентом нет (D-15): `grep -in "workflow\|CLAUDE\|backlog\|agent" shooter/README.md` — без совпадений. Хвостовые пробелы: первая версия ASCII-диаграммы состояний содержала их (скопированы из выравнивания стрелок) — найдены `grep -n " $"` и убраны, повторная проверка → 0 совпадений. Полный гейт: `make check` → exit 0 с первого прогона, 75 тестов без изменений — подтверждает, что задача не изменила поведение. Специального gate у позиции нет. |
| T-TDS-13 | DONE | `shooter` — зависимость `menu/Cargo.toml`; `MenuItem::Shooter` — пятый вариант (`ITEMS: [MenuItem; 5]`, подпись `"SHOOTER"`, перед `QUIT`); `menu/src/main.rs` — ветка `MenuItem::Shooter => { shooter::run().await; next_frame().await; continue; }` с той же ловушкой кадра возврата D-23, что у трёх предыдущих игр; порядок пунктов `PONG`, `ARKANOID`, `ASTEROIDS`, `SHOOTER`, `QUIT`; корневой `README.md` — строка `Game 04 — Top-down Shooter` со ссылкой на каталог (README игры появится в `T-TDS-14`) и статусом «в разработке», `cargo run -p shooter` добавлен в список запусков; заодно исправлена устаревшая строка Asteroids («в разработке» → «завершена и принята», ссылка на её README) — расхождение с фактическим статусом `DONE` в реестре. | RED наблюдался компиляцией: `cargo test -p menu` → exit 101, «non-exhaustive patterns: `MenuItem::Shooter` not covered» сразу после добавления варианта в `enum`/`ITEMS`, до правки `main.rs`. После добавления ветки — ожидаемое падение существующего теста меню (предсказано backlog по прецеденту `T-AST-11`): `quit_confirms_to_the_intent_to_quit` → «подтверждение на пункте QUIT вернуло Shooter, ожидалось Quit» (три `move_down()` при пяти пунктах указывают на `Shooter`, а не на `Quit`) — исправлено четвёртым `move_down()`, это приведение теста к изменившемуся списку, а не ослабление проверки. Новый тест `moving_down_three_items_points_at_shooter` позеленел с первого прогона и закрыт изолирующей мутацией: перестановка `MenuItem::Shooter`/`MenuItem::Asteroids` в `ITEMS` дала exit 101 «подтверждение после трёх move_down вернуло Asteroids, ожидалось Shooter»; мутация откачена, sha256 `selection.rs` (`f9273e9b…`) совпал до/после. Расширение существующего теста `each_item_label_matches_its_name` строкой `Shooter.label()` — не новая проверка, не мутировалось отдельно. GREEN: `cargo test -p menu` → exit 0 (10 passed). Полный гейт: `make check` → exit 0 с первого прогона, 75 тестов shooter + 10 menu + прочие неизменны. **Специальный gate (наблюдаемый прогон `cargo run`: меню → SHOOTER → игра → `Esc` → меню, программа не закрывается) пропущен по решению управляющего 2026-09-25 (default authority, по прецеденту решения владельца для Asteroids); наблюдаемый прогон отложен до конца очереди Game 04.** |
| T-TDS-12 | DONE | `GameState::message(self, wave)` — чистая функция текста по состоянию (WaitingToStart/WaveCompleted/GameOver, Playing → None), латиница; `Effect{position, time_remaining}` — растущий затухающий круг на месте убитого врага, `Game::effects` пополняется из `destroyed_positions` вместе с pickup, тикает и чистится безусловно в начале `update()` (кроме GameOver, где мир заморожен); `Game::is_invulnerable()` — видимый предикат для отрисовки. `run()`: HUD (`Health`/`Ammo`/`Score`/`Wave`) рисуется в экранных координатах после `set_default_camera` (порядок D-39 с `T-TDS-3`); сообщение состояния центрируется по окну; игрок во время неуязвимости рисуется тусклее. | RED наблюдался по одному тесту, `cargo test -p shooter -- each_state_shows_its_own_message` → exit 101 на стаб-`message` (всегда `None`) — «assertion `left == right` failed» на WaitingToStart. Три теста написаны вместе с реализацией и прошли с первого прогона, закрыты тремя изолирующими мутациями при exit 0 контрольных тестов: `is_invulnerable` захардкожен в `false` — «касание не включило неуязвимость: is_invulnerable() = false»; снятие `self.effects.push(Effect{...})` — «убийство не оставило эффект»; `retain(|effect| ...)` заменён на `retain(|_| true)` — «эффект пережил свою длительность: осталось 1». Мутации откачены, sha256 `game.rs` (`b9c22bd6…`) совпал до/после (до `make format`). GREEN: `cargo test -p shooter --lib` → exit 0 (75 passed). Точечно: `make test-target TEST=message` → exit 0. Полный гейт: `make check` → exit 0 (первый прогон exit 2 на `cargo fmt --check`, исправлено `make format`; второй прогон → exit 0), 75 тестов. Специального gate у позиции нет; вид экрана подтверждается наблюдением на `T-TDS-15`. |
| T-TDS-11 | DONE | `GameState` получил `WaveCompleted`; контактный урон (`tick` неуязвимости + `check_player_collision`) переставлен перед `combat::resolve` — враг, который в этом же кадре умирает от пули, успевает нанести касание игроку до того, как исчезнет из коллекции, поэтому одновременная смерть последнего врага и смертельный урон игроку дают `GameOver`, а не `WaveCompleted` (проверка `if self.state == GameState::Playing && self.enemies.is_empty()` стоит последней и не срабатывает, если `GameOver` уже выставлен). `Space` в `WaveCompleted` вызывает `advance_wave()`: номер волны растёт, `enemy::spawn_wave` даёт больше врагов, пули очищаются, счёт/здоровье/патроны не трогаются — поля матча, а не волны. | RED наблюдался по пяти тестам из семи одним прогоном на стаб-заглушках (переход в `WaveCompleted` закомментирован, `advance_wave` — no-op), `cargo test -p shooter -- destroying_the_last_enemy the_wave_does_not_end enemies_do_not_spawn the_next_wave advancing_the_wave game_over_at_the_same_moment` → exit 101: «опустевшая коллекция не завершила волну: состояние Playing»; «номер волны после перехода — 1, ожидалось 2»; «следующая волна дала 0 врагов, предыдущая — 0»; «счёт/здоровье/патроны» (advancing-тест провалился на состоянии); «смерть последнего врага и урон игроку в одном кадре не дали GameOver: состояние Playing». Два теста (`the_wave_does_not_end_while_an_enemy_remains`, `enemies_do_not_spawn_on_their_own_in_wave_completed`) позеленели на стабе с первого прогона и закрыты изолирующими мутациями при exit 0 контрольных тестов: снятие условия `self.enemies.is_empty()` (безусловный переход в `WaveCompleted` в `Playing`) дало «волна завершилась при оставшемся враге»; безусловный вызов `advance_wave()` без проверки `input.space_pressed` дал «враги появились в WaveCompleted без подтверждения игрока». После реализации тест на одновременность GameOver/WaveCompleted вскрыл реальную проблему очерёдности: контактный урон стоял после `combat::resolve`, поэтому враг, убитый пулей в этом кадре, уже не мог быть учтён в контактной проверке — порядок переставлен, и тест дважды наблюдал честный RED («состояние Playing», затем «состояние WaveCompleted») до финального исправления порядка. Побочный эффект перестановки: тесты боеприпасов (`the_last_bullet_fires...`) и pickup-интеграции (`the_fourth_kill_leaves_a_pickup...`) обнаружили, что их вспомогательные функции (`playing_game()` без врагов, повторное убийство единственного врага без сброса состояния) начали неявно завершать волну — оба зафиксированы через явную правку тестовых хелперов (добавлен далёкий враг в `playing_game()`; явный возврат `game.state = GameState::Playing` перед каждым убийством в pickup-тесте), а не через ослабление проверяемого поведения. Мутации откачены, sha256 `game.rs` (`b1b22a5f…`) совпал до/после (до `make format`). GREEN: `cargo test -p shooter --lib` → exit 0 (72 passed). Точечно: `make test-target TEST=wave` → exit 0. Полный гейт: `make check` → exit 0 (первый прогон exit 2 на `cargo fmt --check`, исправлено `make format`; второй прогон → exit 0), 72 теста. Специального gate у позиции нет; прохождение минимум двух волн подтверждается наблюдением на `T-TDS-15`. |
| T-TDS-10 | DONE | `combat::resolve` теперь возвращает позиции уничтоженных врагов (первый потребитель вне тестов); `pickup.rs` — `Pickup{position, kind, radius, time_to_live}`, `enum PickupKind{Health, Ammo}`, `pickup_for_kill(kill_number)` детерминированное правило (D-37: 4-й убитый → Health, 2-й/не 4-й → Ammo, остальные — ничего) через `.is_multiple_of()`; `pickup::resolve` — подбор circle-to-circle, `Health` не выше максимума, `Ammo` без потолка, подобранный pickup удаляется `retain`; `pickup::remove_expired` — lifetime 8с. `Game` получил `pickups: Vec<Pickup>` и приватный счётчик `kills`; в `Playing` после `combat::resolve` каждая уничтоженная позиция увеличивает `kills` и, по правилу, спавнит pickup; `run()` рисует pickups кругом своего цвета (зелёный — Health, жёлтый — Ammo). Интеграционный тест `game::` (`the_fourth_kill_leaves_a_pickup_at_the_dead_enemys_position`) закрывает явно названный в backlog пункт «смерть врага по правилу выпадения оставляет pickup в его позиции» через реальный `Game::update`, а не только через чистую функцию `pickup_for_kill`. | RED наблюдался явно только для двух новых тестов `combat::` (`a_kill_reports_the_dead_enemys_position`, `a_frame_without_a_kill_reports_no_positions`) через промежуточный стаб (`destroyed_positions.push` временно снят) — оба обязаны были провалиться и первый действительно провалился, «assertion `left == right` failed» на пустом векторе; расширение сигнатуры `combat::resolve` — рефакторинг (6 старых тестов остались зелёными `cargo test -p shooter -- combat::` → exit 0 до и после). Девять тестов `pickup::` написаны вместе с реализацией и все прошли с первого прогона (`cargo test -p shooter -- pickup::` → exit 0), поэтому закрыты шестью изолирующими мутациями при exit 0 контрольных тестов (`the_rule_is_deterministic` и `a_frame_without_a_kill...` не мутировались — чистые функции, детерминизм и пустой результат гарантированы структурой кода): `%4`→`%5` поймала `the_fourth_kill_drops_health`; `%2`→`%3` поймала `the_second_kill_drops_ammo`; снятие `.min(max_health)` поймала health-cap-тест; `AMMO_RESTORE`→`AMMO_RESTORE+1` поймала ammo-тест; `!touched`→постоянный `true` в `retain` поймала тест повторного подбора; `is_expired` захардкожен в `false` поймал lifetime-тест. Мутации откачены, sha256 `pickup.rs` (`fea3f2f4…`) и `combat.rs` (`4ff068b1…`) совпали до/после (до `make format`/clippy-правки `.is_multiple_of()`, семантически эквивалентной). Интеграционный тест `the_fourth_kill_leaves_a_pickup_at_the_dead_enemys_position` наблюдал честный RED дважды при первой формулировке: первый прогон дал «2 pickups» вместо ожидаемого 1 (второе убийство тоже даёт `Ammo`-pickup — тест переписан на поиск по виду `Health`, а не по счётчику), второй прогон дал расхождение позиции («появился в (401.4, 400.5), ожидалось (400, 400)») — враг успевает сделать шаг chase AI в том же кадре, где его убивает пуля; тест переписан на допуск 5.0 и уникальные позиции на каждой итерации. Мутация `self.kills` → `self.kills + 1` в аргументе `pickup_for_kill` подтвердила, что переписанный тест ловит реальную логику (exit 101, «pickups вида Health после четырёх убийств: 0, ожидался 1»), не пойманная первой (наивной) версией теста с одной общей позицией; мутация откачена, sha256 `game.rs` совпал (`6bef5e20…`) до/после. GREEN: `cargo test -p shooter --lib` → exit 0 (65 passed). Точечно: `make test-target TEST=pickup` → exit 0. Полный гейт: `make check` → exit 0 (первый прогон exit 2 на `cargo fmt --check`, исправлено `make format`; второй прогон exit 2 на clippy `manual_is_multiple_of` — заменено на `.is_multiple_of()`; третий прогон → exit 0), 65 тестов. Специального gate у позиции нет. |
| T-TDS-9 | DONE | `Game::ammo: i32`, начальный запас `START_AMMO = 40` (при cooldown 0.25с — около 10с непрерывной стрельбы, укладывается в сеанс в несколько минут вместе с остальным чек-листом). Проверка `self.ammo > 0` стоит до вызова `bullet::shoot`, а не внутри него: при нуле патронов cooldown не трогается — ЛКМ вхолостую не запускает паузу между выстрелами. Выстрел уменьшает запас на единицу только вместе с фактическим созданием пули (`if let Some(new_bullet) = ...`). | RED наблюдался по трём тестам из четырёх, `cargo test -p shooter -- firing_a_shot firing_with_zero_ammo ammo_never_goes_negative the_last_bullet_fires` → exit 101: «запас после выстрела — 40, ожидалось 39»; «выстрел при нуле патронов создал пулю»; «выстрел при нуле патронов после последнего создал вторую пулю: left 2, right 1»; четвёртый (`ammo_never_goes_negative`) позеленел на неизменённой логике с первого прогона (стрельба без проверки запаса не уходила в минус при этом сценарии случайно) и закрыт изолирующей мутацией: снятие `&& self.ammo > 0` дало exit 101 «запас патронов ушёл в минус: -1» при двух выстрелах с ammo=1 через истёкший cooldown; мутация откачена, sha256 `game.rs` совпал (`cbd176f4…`) до/после (до `make format`). GREEN: `cargo test -p shooter -- game::` → exit 0 (11 тестов game, включая 4 новых). Точечно: `make test-target TEST=ammo` → exit 0. Полный гейт: `make check` → exit 0 (первый прогон exit 2 на `cargo fmt --check`, исправлено `make format`; второй прогон → exit 0), 53 теста. Специального gate у позиции нет; расход патронов до нуля подтверждается наблюдением на `T-TDS-15`. |
| T-TDS-8 | DONE | Крейт получил `Game` (D-35): собирает арену, игрока, пули, cooldown, врагов, счёт, состояние, здоровье, таймер неуязвимости, номер волны в одном месте; `run()` перестал держать их локальными переменными по отдельности и работает через `Game::update(&Input, delta_time)` + `Game::restart()`. `enum GameState{WaitingToStart, Playing, GameOver}` (простой enum, без framework); в `WaitingToStart` игрок двигается/целится, `Space` переводит в `Playing` (решение по прецеденту D-20, backlog не называл сигнал явно); контактный урон — `circles_overlap(player, enemy)` в `check_player_collision`, одно касание за кадр даже от нескольких врагов сразу (`any()`); неуязвимость — таймер, тикающий отдельным шагом до проверки (не внутри неё), так что при большом `delta_time` истечение и новое попадание засчитываются в одном кадре; `GameOver` при здоровье ≤0, мир не обновляется (ранний `return` в `update`); `restart()` — пересоздание `Game::new(self.arena)` из любого состояния. | RED неприменим впрямую: все 7 тестов `game::` написаны вместе с реализацией и прошли с первого прогона (`cargo test -p shooter -- game::` → exit 0), поэтому все 7 закрыты изолирующими мутациями по одной за раз при exit 0 контрольных тестов: снятие `self.health -= CONTACT_DAMAGE` («здоровье после касания» не изменилось); снятие guard `invulnerability_timer > 0.0` («повторное касание в пределах cooldown нанесло урон»); замена `*timer -= delta_time` на вариант, не уходящий ниже `0.0001` («касание после истечения cooldown не нанесло урон»); замена `any()`+фиксированный урон на `filter().count()`×урон («два одновременных касания нанесли не одно попадание»); снятие раннего `return` при `GameOver` — поймана паникой на `unreachable!()`, что тоже честный сигнал: тест `zero_health_ends_the_game_and_freezes_the_world` упал; `restart()` заменён на смену одного поля состояния вместо пересоздания — упали оба теста рестарта («счёт после рестарта», «состояние после рестарта из Playing»). Все мутации откачены, sha256 `game.rs` совпал (`d167137b…`) до/после каждой серии. GREEN: `cargo test -p shooter --lib` → exit 0 (49 passed). Точечно: `make test-target TEST=game` → exit 0. Полный гейт: `make check` → exit 0 (первый прогон exit 2 на clippy `-D dead-code` для `Game::wave` — добавлен `#[allow(dead_code)]` с комментарием о первом потребителе `T-TDS-11`; второй прогон → exit 0), 49 тестов (12 player + 7 camera + 7 bullet + 5 enemy + 3 collision + 6 combat + 2 score + 7 game). Специального gate у позиции нет. |
| T-TDS-7 | DONE | `Score{value}` (`new`/`add`/`value`, первый потребитель `value()` вне тестов — UI на `T-TDS-12`); `collision::circles_overlap` — единственная на крейт проверка круг-с-кругом (D-38), сравнение квадратов без корня; `combat::resolve(bullets, enemies, score)` — обход «враги снаружи, пули внутри» с флагом `consumed` на пулю (пуля не бьёт дважды за кадр и не переиспользуется на двух врагов), урон -1 за попадание, `retain` по здоровью ≤0 с подсчётом уничтоженных для одного начисления очков, `retain` пуль по тому же флагу `consumed`. `run()` вызывает `combat::resolve` в стадии 4 (столкновения). | RED неприменим впрямую: тесты (11: 3 collision + 6 combat + 2 score) написаны вместе с реализацией и все прошли с первого прогона (`cargo test -p shooter -- combat:: collision:: score::` → exit 0), поэтому все 11 закрыты изолирующими мутациями по одной за раз при exit 0 контрольных тестов: `<=`→`<` и безусловный `true` в `circles_overlap` поймали оба граничных collision-теста; `add` заменён на инкремент константой и начальное значение `0`→`5` поймали оба score-теста; в `combat::resolve` — снятие `enemy.health -= BULLET_DAMAGE` («здоровье после попадания» не изменилось), `<=0`→`<0` («враг пережил своё N-е попадание»), `destroyed * ENEMY_KILL_POINTS * 2` («счёт после одной смерти — 20, ожидалось 10»), снятие `consumed[bullet_index] = true` («одна пуля повредила 2 врагов»), удаление финального `retain` пуль («удалённая пуля нанесла урон во втором кадре») — каждая мутация подтверждена контрольным падением соответствующего теста и откачена; sha256 всех трёх файлов совпал до/после каждой серии мутаций (после `make format`). GREEN: `cargo test -p shooter --lib` → exit 0 (42 passed). Точечно: `make test-target TEST=combat` → exit 0. Полный гейт: `make check` → exit 0 (первый прогон exit 2 на `cargo fmt --check`, исправлено `make format`; второй прогон exit 2 на clippy `-D dead-code` для `Score::value()` — добавлен `#[allow(dead_code)]` с комментарием о первом потребителе `T-TDS-12`; третий прогон → exit 0), 42 теста (12 player + 7 camera + 7 bullet + 5 enemy + 3 collision + 6 combat + 2 score). Специального gate у позиции нет. |
| T-TDS-6 | DONE | `Enemy{position, health = ENEMY_HEALTH(3), radius}`; `chase(target, delta_time)` — нормализованный вектор к цели × скорость × delta time, `try_normalize()` на нулевом расстоянии не даёт `NaN` и оставляет врага на месте (тот же приём, что `Player::aim_at`); `spawn_wave(wave, arena, player_position)` детерминирована (D-37): позиция по индексу и номеру волны на одном из четырёх краёв арены (`position_on_perimeter`), отодвинутая от игрока минимум на 300px вдоль той же прямой, если оказалась ближе (`push_away_from_player`) — расстояние остаётся воспроизводимым, а не отбраковкой со вторым броском. `run()` спавнит первую волну при старте, гонит врагов к игроку в стадии обновления, рисует кругом другого цвета. `health` первым потребителем вне тестов получит `T-TDS-7`, помечено `#[allow(dead_code)]`. | RED наблюдался одним прогоном по всем 5 тестам на стаб-реализации (`chase` — no-op, `spawn_wave` — пустой вектор), `cargo test -p shooter -- enemy::` → exit 101, 3 из 5 упали по своей причине («расстояние после шага — …», «60 кадров по 1/60: x = 0, ожидалось 90», «волна не дала врагов, тест некорректен»); два теста (стоп на цели без NaN, детерминизм повтора) позеленели на стабе с первого прогона и закрыты двумя изолирующими мутациями при exit 0 контрольных тестов: `try_normalize()`+ранний `return` заменён на `unwrap_or(Vec2::X)` — «враг сдвинулся на нулевом расстоянии до цели»; в `position_on_perimeter` добавлен статический `AtomicU32`-счётчик вызовов, участвующий в формуле сдвига — «assertion failed» на сравнении позиций первого и второго вызова с тем же номером волны. Обе мутации откачены, sha256 `enemy.rs` совпал (`7c7352a7…`) до/после (до `make format`). GREEN: `cargo test -p shooter -- enemy::` → exit 0 (5 passed). Точечно: `make test-target TEST=enemy` → exit 0. Полный гейт: `make check` → exit 0 (первый прогон exit 2 на `cargo fmt --check`, исправлено `make format`; второй прогон exit 2 на clippy `-D dead-code` для `Enemy::health` — добавлен `#[allow(dead_code)]` с комментарием о первом потребителе; третий прогон → exit 0), 31 тест (12 player + 7 camera + 7 bullet + 5 enemy). Специального gate у позиции нет. |
| T-TDS-5 | DONE | `Bullet{position, direction, time_to_live}`, `Vec<Bullet>` и `shoot_cooldown: f32` — локальные переменные в `run()` (D-40, Game ещё не введён — появится в `T-TDS-8`); `bullet::shoot` спавнит пулю у `Player::barrel_position()` (новый общий метод, рефакторинг без изменения поведения — линия ствола на отрисовке теперь тоже через него) с направлением, зафиксированным в момент выстрела; `tick_cooldown`/`remove_expired` — свободные функции по прецеденту Asteroids; удаление по lifetime и выходу за арену (край, не wrap — D-41) одним `retain`. ЛКМ (`is_mouse_button_down(MouseButton::Left)`) читается в стадии ввода, пули двигаются и чистятся в стадии обновления, рисуются кругом (D-38) после игрока. | Рефакторинг `Player::barrel_position()` — не новое поведение: `cargo test -p shooter --lib` → exit 0 (19 passed) до и после извлечения. RED наблюдался одним прогоном по всем 7 тестам bullet на стаб-реализации (`advance`/`tick_cooldown` — no-op, `is_expired`/`is_outside` — `false`, `shoot` — всегда `None`), `cargo test -p shooter -- bullet::` → exit 101, 6 из 7 упали по своей причине («первый выстрел не создал пулю», «пуля истекла раньше своего lifetime» после not-expired проверки не сработала и т.д.); седьмой (`a_bullet_inside_the_arena_is_not_outside`) позеленел на стабе `false` с первого прогона и закрыт изолирующей мутацией `is_outside` → безусловный `true` дал exit 101 «пуля внутри арены распознана как вышедшая»; мутация откачена, sha256 `bullet.rs` совпал (`ec247c…`) до/после (до `make format`). GREEN: `cargo test -p shooter -- bullet::` → exit 0 (7 passed). Точечно: `make test-target TEST=bullet` → exit 0. Полный гейт: `make check` → exit 0 (первый прогон exit 2 на `cargo fmt --check`, исправлено `make format`; второй прогон → exit 0), 26 тестов (12 player + 7 camera + 7 bullet). Специального gate у позиции нет. |
| T-TDS-4 | DONE | `Player` получил `aim_angle`, `facing()` (единственный переход «угол → направление», прецедент `Ship::facing()`) и `aim_at(cursor_world)`: `direction`/`normalized_direction`/`angle` — три именованных выражения; курсор на игроке (`try_normalize()` → `None`) не меняет `aim_angle`. `run()` читает `mouse_position()`, переводит в мировые координаты через `camera::screen_to_world` (первый потребитель вне тестов) с тем же `camera_center`, что и рендеринг, и вызывает `aim_at` в стадии обновления, до столкновений и рендеринга. Отрисован ствол — линия от центра игрока в направлении `facing()`. | RED наблюдался по двум тестам из пяти на стаб-реализации (`facing()` → `Vec2::X`, `aim_at` — no-op), `cargo test -p shooter -- player::` → exit 101: «курсор снизу дал направление (1, 0), ожидалось (0, 1)» и «facing() дал (1, 0), ожидалось направление курсора (-0.857…, 0.514…)»; три теста (право, единичная длина, курсор-на-игроке) позеленели на стабе с первого прогона и закрыты тремя изолирующими мутациями при exit 0 контрольных тестов: перестановка `atan2(x, y)` вместо `atan2(y, x)` уронила cursor-right-тест («курсор справа дал направление (0, 1), ожидалось (1, 0)»); домножение `facing()` на `1.5` уронило тест единичной длины; замена `try_normalize()`+ранний `return` на `normalize_or_zero()` без пропуска уронила тест «курсор на игроке» (после правки теста на ненулевой `angle_before`, иначе совпадение с 0.0 маскировало мутацию) — «курсор на игроке изменил ориентацию: было 1.5707964, стало 0». Все три мутации откачены, sha256 `player.rs` совпал (`2b1c46…`) до/после каждой. GREEN: `cargo test -p shooter -- player::` → exit 0 (12 из 12). Точечно: `make test-target TEST=cursor` → exit 0 (2 из 19). Полный гейт: `make check` → exit 0 с первого прогона, 19 тестов (12 player + 7 camera). Специального gate у позиции нет; прицеливание подтверждается наблюдением на `T-TDS-15`. |
| T-TDS-3 | DONE | Камера — чистые функции `camera_center`/`screen_to_world`/`world_to_screen` в `camera.rs`, единственный перевод «экран ↔ мир» на крейт (D-39); `camera_center` следует за игроком и клампится краями арены с поправкой на половину экрана; `run()` ставит `Camera2D` (`target = camera_center`, `zoom = (2/w, -2/h)` для y-down) до рендеринга мира и `set_default_camera()` после — камера встаёт раньше мыши (`T-TDS-4`), как того требует D-39. `screen_to_world`/`world_to_screen` пока не вызываются из `run()` (первый потребитель — `T-TDS-4`), поэтому помечены `#[allow(dead_code)]` с комментарием. | RED наблюдался одним прогоном по всем 7 тестам на стаб-функциях (identity/`Vec2::ZERO`), `cargo test -p shooter -- camera::` → exit 101, 6 из 7 упали по своей причине («камера у левого края в x = 0, ожидалось 480» и т.д.); седьмой (`round_tripping_...`) позеленел на стаб-идентичности с первого прогона и закрыт изолирующей мутацией `world_to_screen`: `-camera_center` → `+camera_center` дало exit 101 после реализации; мутация откачена, sha256 `camera.rs` совпал (`3c78a8…`) до/после (до `make format`). GREEN: `cargo test -p shooter -- camera::` → exit 0 (7 passed). Точечно: `make test-target TEST=camera` → exit 0. Полный гейт: `make check` → exit 0 (первый прогон exit 2 на `cargo fmt --check`, исправлено `make format`; второй прогон exit 2 на clippy `-D dead-code` для обеих функций перевода — добавлен `#[allow(dead_code)]` с комментарием о первом потребителе; третий прогон → exit 0). Специального gate у позиции нет; следование камеры подтверждается наблюдением на `T-TDS-15`. |
| T-TDS-2 | DONE | Игрок как данные (`position`, `radius = 16`) ходит по WASD со скоростью 250 px/s без инерции (скорость присваивается из ввода, комментарий фиксирует отличие от корабля Asteroids); намерение движения нормализуется через `normalize_or_zero` (диагональ не быстрее прямого, нулевой/взаимно гасящий ввод не даёт `NaN`); `clamp_to_arena` удерживает позицию внутри арены с поправкой на радиус; игрок нарисован кругом с точкой в центре; `run()` читает WASD и вызывает `update`/`clamp_to_arena` в стадиях 1 и 3. | RED наблюдался по первому тесту, `cargo test -p shooter -- moving_right_for_a_second` → exit 101 (стаб-`update` не двигал игрока: «x = 960, ожидалось 1210»); после реализации движения и нормализации все 7 тестов позеленели одним прогоном (`cargo test -p shooter -- player::` → exit 0). Шесть тестов, зелёных с первого запуска после реализации, закрыты тремя изолирующими мутациями при exit 0 контрольных тестов: снятие `.normalize_or_zero()` (возврат нормализованного вектора) дало `диагональное движение прошло 353.55, прямое — 250` (ловит diagonal-тест); замена `.normalize_or_zero()` на `.normalize()` дала NaN на нулевом векторе — `opposite_keys_together` упал на `is_finite()`; замена `clamp(self.radius, …)` на `clamp(0.0, …)` (снятие поправки на радиус) уронила разом все четыре граничных теста («игрок ушёл за левую границу: x = -50, ожидалось 16» и симметрично для остальных трёх). Все мутации откачены, sha256 `shooter/src/player.rs` совпал (`5c74bf…` до/после нормализации, `2885b1…` после `make format`). GREEN: `cargo test --manifest-path ./Cargo.toml -p shooter` → exit 0 (7 passed). Точечно: `make test-target TEST=player` → exit 0. Полный гейт: `make check` → exit 0 (первый прогон exit 2 на `cargo fmt --check`, исправлено `make format`; второй прогон → exit 0). Специального gate у позиции нет; управление подтверждается наблюдением на `T-TDS-15`. |
| T-TDS-1 | DONE | Крейт `shooter` пятым участником workspace открывает окно 960×600 с ареной 1920×1200 (арена вдвое шире и выше окна — camera follow на `T-TDS-3` будет наблюдаемым), стадии кадра D-09 видны как структура `run()`, `Esc` закрывает игру, `Arena` объявлена в этом крейте (дублирование `Field` — цена D-02). | RED наблюдаемый без теста: `cargo test --manifest-path ./Cargo.toml -p shooter` → exit 101, `package ID specification 'shooter' did not match any packages`. GREEN: та же команда → exit 0 (0 тестов — окно и отрисовка не тестируются, D-10). Полный гейт: `make check` → exit 0 с первого прогона. **Специальный gate (наблюдаемый прогон `cargo run -p shooter`) пропущен по решению управляющего 2026-09-25 (default authority, по прецеденту решения владельца для Asteroids); наблюдаемый прогон отложен до конца очереди Game 04.** |
| T-PONG-1 | DONE | Cargo-проект Pong живёт в корне репозитория и открывает окно с полем: Macroquad подключён, game loop показывает стадии кадра явно, поле с рамкой и пунктирной центральной линией нарисовано, `Esc` закрывает игру. | RED: `make test` → exit 2, `manifest path .../Cargo.toml does not exist`. GREEN: `make test` → exit 0. Полный гейт: `make check` → exit 0. Unit-тестов нет по D-10 (рендеринг и ввод не тестируются). Специальный gate: прогон `cargo run` выполнял владелец, а не агент; gate подтверждён владельцем, дословный ответ — «ага, видно». |
| T-PONG-2 | DONE | Обе ракетки существуют как данные и движутся по вертикали через delta time с ограничением границами поля: логика ракетки вызывается из тестов без окна, `W`/`S` и `↑`/`↓` привязаны в стадии чтения ввода, обе ракетки нарисованы. | RED наблюдался по одному тесту за цикл, `make test-target TEST=<имя>` → exit 2 каждый раз: движение отсутствовало (`y = 250, ожидалось 450`); перемещение игнорировало направление (`секунда без ввода сдвинула ракетку: y = 450.00058`); не было верхней границы (`y = -149.99986, ожидалось 0`); не было нижней (`y = 649.99994, ожидалось 500`). GREEN: `make test` → exit 0 (4 passed). Точечно: `make test-target TEST=paddle` → exit 0. Полный гейт: `make check` → exit 0. Привязка клавиш и отрисовка unit-тестами не покрыты по D-10; фактическое управление подтверждается наблюдаемым прогоном в `T-PONG-8`. |
| T-PONG-3 | DONE | Мяч существует как данные и движется по вектору скорости через delta time, отражаясь от верхней и нижней границ поля с возвратом внутрь поля (без дребезга у границы); столкновение с границами занимает стадию проверки столкновений в кадре, мяч нарисован квадратом по D-12. | RED наблюдался по одному циклу за раз, `make test-target TEST=<имя>` → exit 2: движения не было (`шаг не сдвинул мяч по горизонтали: x = 100, ожидалось 250`); не было отражения от верхней границы (`velocity_y = -400, ожидалось 400`); не было от нижней (`velocity_y = 400, ожидалось -400`). Два теста позеленели с первого запуска и их RED получен мутацией: фиксированный шаг `1/60` вместо delta time дал `120 кадров по 1/120: мяч в (700, 399.9993), ожидалось (400, 300)`, а подмена условий границ на знак скорости дала `мяч внутри поля отразился: velocity_y = 100, ожидалось -100` и симметрично для нижней ветки; мутации откачены, sha256 файла совпал с исходным. GREEN: `make test` → exit 0 (9 passed). Точечно: `make test-target TEST=ball` → exit 0 (5 passed). Полный гейт: `make check` → exit 0 (первый прогон exit 2 на `cargo fmt --check`, форматирование исправлено). Отрисовка мяча unit-тестом не покрыта по D-10. |
| T-PONG-4 | DONE | Мяч отражается от обеих ракеток по самостоятельной проверке пересечения прямоугольников: горизонтальная скорость меняет знак, вертикальная вычисляется из места попадания относительно центра ракетки (верх — вверх, центр — прямо, низ — вниз), мяч, летящий от ракетки, второй раз не отражается и не остаётся внутри неё. | RED наблюдался по одному циклу за раз, `make test-target TEST=<имя>` → exit 2: `левая ракетка не отправила мяч вправо: velocity_x = -300, ожидалось 300`; `правая ракетка не отправила мяч влево: velocity_x = 300, ожидалось -300`; `попадание в центр ракетки увело мяч по вертикали: velocity_y = 100, ожидалось около 0`; `попадание выше центра не отправило мяч вверх: velocity_y = 0, ожидалось -150`; `промах мимо ракетки из (50, 100) изменил скорость: velocity = (300, -300), ожидалось (-300, 100)`; `мяч, летящий от ракетки, отразился второй раз: velocity = (-300, 0), ожидалось (300, 100)`; `мяч остался внутри левой ракетки: x = 50, ожидалось не левее 56`. Один тест (`hit_below_the_paddle_center_sends_the_ball_down`) позеленел с первого запуска, его RED получен изолирующей мутацией `clamp(-1.0, 1.0)` → `clamp(-1.0, 0.0)`: `попадание ниже центра не отправило мяч вниз: velocity_y = 0, ожидалось 150` при exit 0 контрольного теста попадания выше центра; мутация откачена, sha256 файла совпал с исходным. GREEN: `make test` → exit 0 (17 passed). Точечно: `make test-target TEST=paddle` → exit 0 (12 passed), `make test-target TEST=bounce` → exit 0 (7 passed). Полный гейт: `make check` → exit 0. Скорость мяча от отскока к отскоку не растёт: `|velocity_x|` — инвариант отскока, а вертикальная составляющая задана его долей. Вызов в кадре и отрисовка unit-тестами не покрыты по D-10. |
| T-PONG-5 | DONE | Вылет мяча за боковую границу приносит очко противнику и возвращает мяч в центр поля: счёт двух игроков живёт как данные в `Score`, гол засчитывается только за полностью вышедший мяч, направление подачи чередуется от сброса к сбросу (D-11), конструктор мяча выражен через тот же сброс, счёт нарисован текстом по обе стороны от центральной линии. | RED наблюдался по одному циклу за раз, `make test-target TEST=<имя>` → exit 2: `мяч за левой границей не засчитан как гол`; `мяч за правой границей не засчитан как гол`; `после сброса мяч не в центре по горизонтали: x = 100, ожидалось 470`; `подача не сменила направление: первая velocity_x = 300, вторая velocity_x = 300`. Тест `ball_inside_the_field_scores_nothing` позеленел с первого запуска, его RED получен двумя изолирующими мутациями по одной за раз: `ball.x + ball.size < 0.0` → `ball.x < 0.0` дало `мяч в точке x = -8 засчитан как гол`, `ball.x > field.width` → `ball.x + ball.size > field.width` дало `мяч в точке x = 952 засчитан как гол`, при exit 0 контрольного теста соответствующей границы в обоих случаях; мутации откачены, sha256 файла совпал с исходным. GREEN: `make test` → exit 0 (22 passed). Точечно: `make test-target TEST=score` → exit 0 (3 passed), `make test-target TEST=reset` → exit 0 (2 passed). Полный гейт: `make check` → exit 0 (первый прогон exit 2, `constant SERVE_RIGHT is never used` под `-D dead-code`, неиспользуемая константа удалена). Отрисовка счёта и вызов проверки гола в кадре unit-тестами не покрыты по D-10. |
| T-PONG-6 | DONE | Игра проходит цикл «ожидание — раунд — гол — ожидание» и начинается заново по сигналу рестарта: `GameState` — простой `enum` из трёх вариантов, `Game` собирает поле, ракетки, мяч, счёт и состояние в одном месте, переходы видны целиком в `Game::update`, `PointScored` живёт ровно один кадр без таймера (D-14), рестарт выражен через конструктор матча, а ракетки по решению задачи управляются во всех состояниях. | RED наблюдался по одному циклу за раз, `make test-target TEST=<имя>` → exit 2: `сигнал старта не начал раунд: состояние WaitingToStart`; `левая ракетка не сдвинулась вверх до подачи: y = 250`; `гол не завершил раунд: состояние Playing`; `засчитанный гол не перевёл игру в ожидание подачи: состояние PointScored`; `сигнал старта в кадре гола пропустил возврат мяча в центр: состояние Playing`; `рестарт из состояния WaitingToStart не обнулил счёт: 3:5`; `мяч сдвинулся до подачи: (642, 402)`. Ни один тест не позеленел с первого запуска, мутационная проверка не потребовалась. GREEN: `make test` → exit 0 (29 passed). Точечно: `make test-target TEST=game` → exit 0 (7 passed). Полный гейт: `make check` → exit 0 (первый прогон exit 2 на `cargo fmt --check`, исправлено `make format`). Привязка `Space` и `R` к клавишам, подсказка на экране и вызов `Game::update` из кадра unit-тестами не покрыты по D-10; текст подсказки латиницей, потому что кириллические глифы встроенного шрифта Macroquad наблюдением не проверены. |
| T-PONG-7 | DONE | Корневой `README.md` объясняет Pong программисту, впервые изучающему game development: семь разделов ТЗ — что это, запуск, таблица управления, что изучается в проекте, архитектура по одной фразе на тип, движение мяча через `position += velocity * delta_time` и столкновение мяча с ракеткой вместе с зависимостью угла от места попадания и защитой от повторного отскока. | RED и GREEN неприменимы: задача не добавляет поведения, новых тестов нет, набор тестов остался прежним (29). Проверка — сверка README с семью разделами ТЗ и с фактическим кодом: `uv run tmp/readme-code-crosscheck.py` → exit 0 (58 фрагментов в обратных кавычках, ни одного идентификатора вне `src/`; сам скрипт — черновик под `tmp/` и не отслеживается), оба блока кода в README сверены дословно с `src/ball.rs` (строки 51–52, 95, 99, 124–127), таблица управления — с привязками клавиш в `src/main.rs`, упоминаний каркаса работы с агентом нет (grep по `workflow`, `make `, `agent`, `CLAUDE` — 0 совпадений, D-15). Полный гейт: `make check` → exit 0 (29 passed). `git diff --check` внутри гейта нового файла не видит, поэтому хвостовые пробелы проверены отдельно: `grep -n " $" README.md` — 0 совпадений и `git diff --cached --check` на застейдженном файле → exit 0. Специального gate у позиции нет. |
| T-PONG-8 | DONE | Аудит подтвердил, что шесть пунктов покрытия D-10 закрыты тестами, а проект остаётся простым по D-02 и D-07 без обязательных внешних assets; единственный найденный пробел — переход `Playing → PointScored` проверялся только со стороны гола — закрыт тестом `a_round_without_a_goal_keeps_playing`, и все критерии приёмки владельца подтверждены. | Покрытие D-10 по пунктам: ограничение ракетки — `paddle_stops_at_the_top_edge`, `paddle_stops_at_the_bottom_edge`; начисление очков — `ball_past_the_left_edge_scores_for_the_right_player`, `ball_past_the_right_edge_scores_for_the_left_player`, `ball_inside_the_field_scores_nothing`; reset мяча — `reset_puts_the_ball_in_the_center_of_the_field`, `serve_direction_alternates_from_reset_to_reset`; изменение направления мяча — `ball_bounces_off_the_top_edge`, `ball_bounces_off_the_bottom_edge`, `ball_inside_the_field_does_not_bounce`; отскок от ракетки — восемь тестов в `src/ball.rs` от `ball_bounces_right_off_the_left_paddle` до `bounce_puts_the_ball_outside_the_paddle`; переходы состояний — восемь тестов в `src/game.rs`. Задача не меняет поведения, поэтому RED единственного дописанного теста получен изолирующей мутацией: безусловное `self.state = GameState::PointScored;` вместо `if self.score.count_goal(...)` прогнано сначала против прежнего набора (`make test` → exit 0, 29 passed — пробел подтверждён), затем против нового теста (`make test-target TEST=a_round_without_a_goal_keeps_playing` → exit 2, `кадр раунда без гола завершил раунд: состояние PointScored`) при exit 0 контрольного `make test-target TEST=a_goal_ends_the_round_with_the_point_scored`; мутация откачена, sha256 `src/game.rs` совпал, `git diff` по файлу не содержит удалённых строк — продакшн-код не изменён. Аудит простоты: `grep` в `src/` по `trait `, `dyn `, `Box<dyn`, `impl Trait`, `<T`, `where `, `unwrap()`, `expect(`, `panic!`, `static mut`, `lazy_static`, `once_cell`, `.clone()`, `load_texture`, `load_sound`, `load_ttf`, `load_file`, `include_bytes` — 0 совпадений; 527 строк продакшн-кода в пяти плоских модулях, одна зависимость `macroquad`. GREEN: `make test` → exit 0 (30 passed). Точечно: `make test-target TEST=game` → exit 0 (8 passed). Полный гейт: `make check` → exit 0 (30 passed; внутри одной цепочкой `cargo fmt --check`, `cargo clippy --all-targets --all-features -- -D warnings`, `cargo test`, `cargo build` — критерии 13–16). Специальный gate: наблюдаемый прогон `cargo run` по чек-листу из 13 шагов выполнял владелец, а не агент; gate подтверждён владельцем, дословный ответ — «все хорошо, все работаетё», замечаний нет. |
| T-ARK-1 | DONE | Репозиторий стал cargo-workspace из двух крейтов: принятый Pong переехал в `pong/` без единого изменения кода и остался зелёным, а новый крейт `arkanoid` открывает окно с полем, показывает стадии кадра D-09 как структуру кода и закрывается по `Esc`. | RED наблюдаемый, теста не потребовалось: `cargo test --manifest-path ./Cargo.toml -p arkanoid` → exit 101, `error: package ID specification 'arkanoid' did not match any packages`; GREEN — та же команда → exit 0 после создания крейта. Новых unit-тестов нет: всё, что создано здесь, — окно и отрисовка, а они не тестируются (D-10). Неизменность кода Pong подтверждена тремя независимыми способами: sha256 пяти `pong/src/*.rs` и `pong/Cargo.toml` совпали с состоянием до `git mv` (`c1cacf04`, `35f703a6`, `98fc6216`, `2a1c07c8`, `f164ea17`, `e84e27a8`); git показал те же файлы как переименования `R100` с нулём изменённых строк в `git show --stat -M`; прямая сверка с принятым состоянием дала `git show HEAD:Cargo.toml | diff - pong/Cargo.toml` → exit 0 и ровно одно отличие в README — разрешённую строку запуска `cargo run` → `cargo run -p pong`. Корректность переезда: `make test` до переезда → exit 0, 30 passed, и после переезда → exit 0, 30 passed из `pong/src`. Точечно: `make test-target TEST=game` → exit 0 (8 passed, 22 filtered out). Полный гейт: `make check` → exit 0 с первого прогона и повторно на закоммиченном состоянии (внутри `cargo fmt --all -- --check`, `cargo clippy --workspace --all-targets --all-features -- -D warnings`, `cargo test --workspace`, `cargo build --workspace` — D-06). Специальный gate: наблюдаемый прогон выполнял владелец, а не агент; подтверждены оба прогона — `cargo run -p arkanoid` (новый крейт: окно, поле, `Esc`) и `cargo run -p pong` (принятая игра пережила переезд без изменений), оба процесса завершились штатно; gate подтверждён владельцем, дословный ответ — «все ок», замечаний нет. |
| T-ARK-2 | DONE | Ракетка игрока Arkanoid существует как данные и ходит по горизонтали через delta time с ограничением обеими боковыми границами поля: логика ракетки вызывается из тестов без окна, `A`/`←` и `D`/`→` привязаны в стадии чтения ввода, ракетка нарисована, а стадии кадра D-09 в `main.rs` заполнены до стадии столкновений. | RED наблюдался по одному тесту за цикл, `make test-target TEST=<имя>` → exit 2 каждый раз: движения не было (`60 кадров по 1/60: x = 400, ожидалось 600`); перемещение игнорировало направление (`секунда без ввода сдвинула ракетку: x = 599.9998, ожидалось 400`); не было левой границы (`ракетка ушла за левую границу: x = -200.00021, ожидалось 0`); не было правой (`ракетка ушла за правую границу: x = 999.9974, ожидалось 860`). Ни один тест не позеленел с первого запуска, мутационная проверка не потребовалась. GREEN: `make test` → exit 0 (4 теста Arkanoid, 30 Pong). Точечно: `make test-target TEST=paddle` → exit 0 (4 из 4 в Arkanoid, 13 из 30 в Pong). Полный гейт: `make check` → exit 0 с первого прогона. Привязка клавиш и отрисовка unit-тестами не покрыты по D-10; фактическое управление подтверждается наблюдаемым прогоном в `T-ARK-12`. |
| T-ARK-3 | DONE | Мяч Arkanoid существует как данные и движется по вектору скорости через delta time, отражаясь от левой, правой и верхней границ с возвратом внутрь поля (без дребезга у границы); нижней стены нет намеренно — ушедший вниз мяч не возвращается до `T-ARK-7`; столкновение со стенами занимает стадию проверки столкновений в кадре, мяч нарисован квадратом по D-12. | RED наблюдался по одному циклу за раз, `make test-target TEST=<имя>` → exit 2: движения не было (`шаг не сдвинул мяч по горизонтали: x = 100, ожидалось 250` и, отдельным прогоном, `60 кадров по 1/60: мяч в (100, 200), ожидалось (400, 300)` — оба теста описывают одну строку перемещения и упали каждый по своему утверждению); не было отражения от левой границы (`левая граница не отразила мяч вправо: velocity_x = -400, ожидалось 400`); от правой (`правая граница не отразила мяч влево: velocity_x = 400, ожидалось -400`); от верхней (`верхняя граница не отразила мяч вниз: velocity_y = -400, ожидалось 400`). Тест `ball_inside_the_field_does_not_bounce` позеленел с первого запуска, его RED получен тремя изолирующими мутациями по одной за раз, каждая — подмена условия положения на знак скорости: `self.x < 0.0` → `self.velocity_x < 0.0` дало `мяч внутри поля отразился: velocity = (100, 0), ожидалось (-100, 0)`; `self.x > rightmost_x` → `self.velocity_x > 0.0` дало `velocity = (-100, 0), ожидалось (100, 0)`; `self.y < 0.0` → `self.velocity_y < 0.0` дало `velocity = (0, 100), ожидалось (0, -100)` — при exit 0 контрольного теста соответствующей границы во всех трёх случаях; мутации откачены, sha256 файла совпал с исходным (`d3b35c6c`). GREEN: `make test` → exit 0 (10 тестов Arkanoid, 30 Pong). Точечно: `make test-target TEST=ball` → exit 0 (6 из 10 в Arkanoid, 20 из 30 в Pong). Полный гейт: `make check` → exit 0 с первого прогона. Отрисовка мяча unit-тестом не покрыта по D-10. |
| T-ARK-4 | DONE | Мяч Arkanoid отражается от ракетки по самостоятельной проверке пересечения прямоугольников: вертикальная скорость меняет знак и уводит мяч вверх, горизонтальная вычисляется из места попадания относительно центра ракетки (левая часть — левее, центр — прямо вверх, правая — правее), мяч на пути вверх второй раз не отражается и не остаётся внутри ракетки. | RED наблюдался по одному циклу за раз, `make test-target TEST=<имя>` → exit 2: `ракетка не отправила мяч вверх: velocity_y = 300, ожидалось -300`; `попадание левее центра не увело мяч влево: velocity_x = 0, ожидалось -150`; `промах мимо ракетки из (100, 530) изменил скорость: velocity = (-300, -300), ожидалось (0, 300)`; `мяч, летящий от ракетки, отразился второй раз: velocity_y = 300, ожидалось -300`; `мяч остался внутри ракетки: нижний край = 550, ожидалось не ниже 540`. Тест `hit_right_of_the_paddle_center_sends_the_ball_right` позеленел с первого запуска, его RED получен изолирующей мутацией `clamp(-1.0, 1.0)` → `clamp(-1.0, 0.0)`: `попадание правее центра не увело мяч вправо: velocity_x = 0, ожидалось 150` при exit 0 контрольного теста попадания левее центра; мутация откачена, sha256 файла совпал с исходным (`64324689`). GREEN: `make test` → exit 0 (16 тестов Arkanoid, 30 Pong). Точечно: `make test-target TEST=bounce` → exit 0 (6 из 16 в Arkanoid, 7 из 30 в Pong). Полный гейт: `make check` → exit 0 с первого прогона. Скорость мяча от отскока к отскоку не растёт: `|velocity_y|` — инвариант отскока, а горизонтальная составляющая задана его долей. Вызов в кадре unit-тестом не покрыт по D-10. |
| T-ARK-5 | DONE | В Arkanoid появилась первая коллекция игровых объектов: `Vec<Brick>` с раскладкой, заданной прямо в коде, AABB-столкновение мяча с блоком со стороной отскока по меньшему перекрытию, уничтожение блока с одного попадания и 100 очков за него; жизненный цикл выражен явно — уничтоженный блок выбывает из столкновений, начисления очков и отрисовки, а за кадр мяч отскакивает не более одного раза. | RED наблюдался по одному циклу за раз, `make test-target TEST=<имя>` → exit 2: `попадание не уничтожило блок: destroyed = false`; `кадр без столкновения уничтожил блок`; `уничтоженный блок не принёс очков: счёт 0, ожидалось 100`; `вход с y = 114 не развернул мяч по вертикали: velocity_y = -300, ожидалось 300`; `вход сбоку не развернул мяч по горизонтали: velocity_x = 300, ожидалось -300`; `уничтоженный блок отразил мяч: velocity_y = 300, ожидалось -300`; `мяч на стыке блоков отразился дважды: velocity = (-100, 300), ожидалось (100, 300)`. Ни один тест не позеленел с первого запуска, мутационная проверка не потребовалась. Обобщение AABB-проверки в `ball.rs` с ракетки до любого прямоугольника — рефакторинг зелёного кода, не новое поведение: 16 тестов Arkanoid и 30 Pong остались зелёными до и после. Последний тест (`a_ball_touching_two_bricks_bounces_once`) дописан сверх списка backlog: без него два отскока за кадр гасят друг друга и мяч проходит сквозь стык блоков — D-18 задаёт минимум, а не потолок. GREEN: `make test` → exit 0 (23 теста Arkanoid, 30 Pong). Точечно: `make test-target TEST=brick` → exit 0 (7 из 23 в Arkanoid). Полный гейт: `make check` → exit 0 (первый прогон exit 2 на `cargo fmt --check`, исправлено `make format`; второй прогон exit 2 на clippy — `the borrowed expression implements the required traits` в `draw_score`, временный `&format!(...)` заменён связанной переменной). Отрисовка блоков и счёта unit-тестом не покрыта по D-10. |
| T-ARK-6 | DONE | У блоков Arkanoid появились три типа простым `enum BrickKind` без иерархий: `Normal` разрушается с одного попадания и даёт 100, `Strong` — со второго и даёт 200, повреждение видно оставшейся прочностью блока и выводится в цвет при отрисовке, `Indestructible` не разрушается никогда, очков не приносит, но отражает мяч как обычная стена; все три типа стоят в раскладке поля. | Рефакторинг перед первым тестом: признак «уничтожен» стал производной от оставшейся прочности (`destroyed()` вместо поля), 23 теста Arkanoid и 30 Pong остались зелёными до и после — поведения это не меняло. RED наблюдался по одному циклу за раз, `make test-target TEST=<имя>` → exit 2: `первое попадание уничтожило прочный блок`; `уничтоженный прочный блок принёс 100 очков, ожидалось 200`; `попадание 1 уничтожило неразрушимый блок`; `неразрушимый блок не отразил мяч: velocity_y = -300, ожидалось 300`. Регрессионный тест `a_normal_brick_is_still_destroyed_by_one_hit_and_scores_one_hundred` описывает поведение, существовавшее до задачи, и позеленел с первого запуска; его RED получен изолирующей мутацией прочности — `Normal` переведён в ветку прочности 2, что дало `обычный блок пережил попадание: осталось прочности 1` при exit 0 контрольного теста `Strong`; мутация откачена, sha256 файла совпал с исходным (`45b7f583`). GREEN: `make test` → exit 0 (28 тестов Arkanoid, 30 Pong). Точечно: `make test-target TEST=brick` → exit 0 (12 из 28 в Arkanoid). Полный гейт: `make check` → exit 0 (первый прогон exit 2 на `cargo fmt --check`, исправлено `make format`). Вывод цвета из типа и прочности unit-тестом не покрыт по D-10; проверяемым остаётся само различие в данных. |
| T-ARK-7 | DONE | Arkanoid стал игрой с циклом «ожидание — раунд — потеря жизни — ожидание»: `Game` собрал поле, ракетку, мяч, блоки, счёт, три жизни и состояние, переходы видны целиком в `Game::update`, мяч до подачи лежит на ракетке и ездит с ней, выход мяча за нижнюю границу списывает жизнь и возвращает мяч на ракетку, при нуле жизней наступает `GameOver`, из которого кадр ничего не меняет, а `Space` начинает раунд из ожидания и возобновляет его после потери жизни (D-20). Объявлены ровно четыре варианта состояния: `LevelCompleted` и `GameWon` без уровней были бы мёртвым кодом и уронили бы гейт под `-D warnings`. | RED наблюдался по одному циклу за раз, `make test-target TEST=<имя>` → exit 2: `сигнал старта не начал раунд: состояние WaitingToStart`; `потеря мяча не отняла жизнь: 3`; `мяч в игре отнял жизнь: осталось 2`; `потеря последней жизни не завершила игру: состояние LifeLost`; `кадр после конца игры сдвинул мяч: (733, 530)`; `сигнал старта не возобновил раунд после потери жизни: состояние LifeLost`. Два теста позеленели с первого запуска, их RED получен изолирующими мутациями по одной за раз. `waiting_to_start_keeps_the_ball_on_the_paddle`: поведение попало в скелет `update` раньше теста, поэтому ветка ожидания заменена на пустую — `мяч не последовал за ракеткой: x = 473` при exit 0 контрольного теста сигнала старта. `the_start_signal_does_not_revive_a_finished_game`: `start_round` сделан безусловным — `сигнал старта воскресил законченную игру: состояние Playing` при exit 0 контрольного теста возобновления после потери жизни. Обе мутации откачены, sha256 файла совпал с исходным (`fd343b4a` и `0f431bd8` соответственно). GREEN: `make test` → exit 0 (36 тестов Arkanoid, 30 Pong). Точечно: `make test-target TEST=game` → exit 0 (8 из 36 в Arkanoid). Полный гейт: `make check` → exit 0 с первого прогона. Привязка `Space` к клавише и отрисовка unit-тестами не покрыты по D-10. |
| T-ARK-8 | DONE | В Arkanoid появились три уровня, заданные прямо в Rust-коде символьными раскладками (`N` / `S` / `I` / `.`), и игра получила конец: уровень завершается, когда не осталось разрушаемых блоков (неразрушимые в подсчёт не входят), `Space` в `LevelCompleted` загружает следующий уровень с сохранением счёта и жизней, завершение последнего уровня даёт `GameWon` сразу, в `GameWon` и `GameOver` кадр ничего не меняет, а `R` полностью перезапускает игру из любого состояния. | Перенос перед первым тестом: раскладка блоков переехала из `brick.rs` в `level.rs` — геометрия сетки и выбор типа по месту это свойство уровня, а не блока; 36 тестов Arkanoid и 30 Pong остались зелёными до и после. RED наблюдался по одному циклу за раз, `make test-target TEST=<имя>` → exit 2: `уничтожение последнего блока не завершило уровень: состояние Playing`; `оставшийся неразрушимый блок не дал завершить уровень: состояние Playing`; `переход не сменил уровень: номер 1`; `последний уровень не привёл к победе: состояние LevelCompleted`; `кадр после победы сдвинул мяч: (733, 530)`; `рестарт из состояния WaitingToStart не вернул игру к началу: 900 очков, 1 жизней, уровень 3`. Ни один тест не позеленел с первого запуска, мутационная проверка не потребовалась. GREEN: `make test` → exit 0 (42 теста Arkanoid, 30 Pong). Точечно: `make test-target TEST=level` → exit 0 (3 из 42 в Arkanoid). Полный гейт: `make check` → exit 0 (первый прогон exit 2 на `cargo fmt --check`; форматирование собрало ряды уровней в одну строку и раскладка перестала быть картинкой, поэтому на `LAYOUTS` поставлен `#[rustfmt::skip]` с объяснением, после чего гейт зелёный). Привязка `R` к клавише unit-тестом не покрыта по D-10. |
| T-ARK-9 | DONE | В Arkanoid появился временный игровой объект: бонус `Wide Paddle` выпадает из каждого четвёртого уничтоженного разрушаемого блока (детерминированно, D-19), падает через delta time, пойманный ракеткой расширяет её на заданное время и исчезает, упущенный исчезает за нижней границей, а потеря жизни, переход на уровень и полный рестарт снимают и падающие бонусы, и эффект, и счётчик до следующего выпадения. | Рефакторинг перед первым тестом: ракетка получила поле оставшегося времени расширения, фикстуры в `paddle.rs` и `ball.rs` обновлены, 42 теста Arkanoid и 30 Pong остались зелёными до и после. RED по своей причине наблюдался у двух тестов, `make test-target TEST=<имя>` → exit 2: `пойманный бонус не расширил ракетку: ширина 100, была 100`; `ширина не вернулась к исходной: 190, ожидалось 120`. Шесть тестов позеленели с первого запуска — часть поведения попала в скелет и в интеграцию с `Game` раньше своих тестов, это сбой последовательности, и он записан здесь как есть; RED каждого получен изолирующей мутацией по одной за раз при exit 0 контрольного теста: фиксированный шаг `FALL_SPEED / 60.0` вместо delta time дал `60 FPS дало y = 290.0001, 120 FPS дало y = 479.99948`; `bonus.y <= field.height` → `true` дало `упущенный бонус остался на поле: 1 штук`; отключение clamp для расширенной ракетки дало `расширенная ракетка вышла за правую границу: правый край = 1189.9973` при exit 0 трёх контрольных тестов; `BONUS_EVERY` 4 → 2 дало `после 2 уничтоженного блока бонусов 1, ожидалось 0`; `breakable_left() < before` → `<= before` дало `удары по неразрушимому блоку уронили бонус: 1 штук`; удаление `self.bonuses.clear()` дало `потеря жизни не убрала падающие бонусы: 1 штук`. Первая попытка мутации для теста расширенной ракетки (`field.width - self.width` → `field.width - WIDTH`) уронила и контрольный тест, поэтому была отброшена как неизолирующая и заменена. Все мутации откачены, sha256 файлов совпал с исходными (`0c1fbe6c`, `cc804be5`, `8f45806e`, `edd05342`, `2464c85f`). GREEN: `make test` → exit 0 (50 тестов Arkanoid, 30 Pong). Точечно: `make test-target TEST=bonus` → exit 0 (6 из 50 в Arkanoid). Полный гейт: `make check` → exit 0 (первый прогон exit 2 на clippy: `manual implementation of .is_multiple_of()` и `very complex type used` на массиве указателей на функции в тесте — первое исправлено вызовом `is_multiple_of`, второе перестроено в три явных блока с общей функцией проверки, после чего мутация для этого теста перепроверена уже против отгружаемой версии). Отрисовка бонусов unit-тестом не покрыта по D-10. |
| T-ARK-10 | DONE | На экране Arkanoid видны счёт, число жизней и номер уровня одной строкой, а каждое состояние показывает своё сообщение по центру поля: `Press SPACE to start` в ожидании подачи и после потери жизни, `LEVEL COMPLETED`, `GAME OVER`, `YOU WIN`; в идущем раунде надписи нет. Выбор сообщения вынесен в чистую функцию `GameState::message`, поэтому соответствие состояния тексту проверяемо, а непокрытой остаётся только отрисовка. | RED: `make test-target TEST=each_state_shows_its_own_message` → exit 2, `состояние WaitingToStart показало None, ожидалось Some("Press SPACE to start")`. Тест сверяется с шестью состояниями и их текстами дословно, включая отсутствие сообщения в `Playing`. GREEN: `make test` → exit 0 (51 тест Arkanoid, 30 Pong). Точечно: `make test-target TEST=message` → exit 0 (1 из 51). Полный гейт: `make check` → exit 0 с первого прогона. Сама отрисовка строки состояния и центрирование сообщения unit-тестом не покрыты по D-10; видимость надписей подтверждается наблюдаемым прогоном в `T-ARK-12`. Текст латиницей: кириллические глифы встроенного шрифта Macroquad наблюдением не проверены. |
| T-ARK-11 | DONE | `arkanoid/README.md` объясняет игру программисту, впервые изучающему game development: все одиннадцать пунктов принятой идеи — запуск, таблица управления, стадии кадра, delta time, коллекция `Vec<Brick>`, жизненный цикл блока и бонуса, AABB и три вида отскока, шесть состояний с переходами, уровни в коде, бонус и раздел о том, чем проект отличается от Pong; корневой указатель называет Arkanoid завершённой игрой со ссылкой в каталог. | RED и GREEN неприменимы: задача не добавляет поведения, новых тестов нет, набор остался прежним (51 тест Arkanoid, 30 Pong). Проверка — сверка каждого утверждения README с исходниками: четыре блока кода сверены дословно (`ball.rs` — движение через delta time, AABB, две строки отскока от ракетки; `level.rs` — раскладка второго уровня), таблица управления сверена с пятью привязками клавиш в `main.rs` (`A`/`Left`, `D`/`Right` через `is_key_down`; `Space`, `R`, `Escape` через `is_key_pressed`), тексты четырёх сообщений — с `GameState::message`, строка статуса — с `draw_status`, очки 100/200/0 — с `BrickKind::points`, три жизни и каждый четвёртый блок — с `LIVES` и `BONUS_EVERY`, три уровня — с длиной `LAYOUTS`, окно 960×600 без изменения размера — с `window_conf`, таблица файлов — с фактическим содержимым `arkanoid/src/`. Упоминаний каркаса работы с агентом нет (D-15): grep по `workflow`, `CLAUDE`, `agent`, `агент`, `backlog` — 0 совпадений. Полный гейт: `make check` → exit 0 с первого прогона. `git diff --check` внутри гейта нового файла не видит, поэтому хвостовые пробелы проверены отдельно: `grep -n " $" arkanoid/README.md` — 0 совпадений и `git diff --cached --check` на застейдженных файлах → exit 0. |
| T-ARK-12 | DONE | Аудит подтвердил полное покрытие D-18 (двенадцать пунктов, ни одного пробела) и отсутствие нарушений простоты, а владелец принял первую версию Arkanoid по сокращённому варианту «по тесту»: запуск, управление, подачу, разрушение блоков, счёт, завершение первого уровня и переход на второй с другой раскладкой подтвердил личным наблюдением, а `GameWon` принял по unit-тесту `T-ARK-8` без ручного прохождения третьего уровня. | Покрытие D-18 по пунктам: 1) ограничение ракетки — `paddle_stops_at_the_left_edge`, `paddle_stops_at_the_right_edge`; 2) потеря жизни — `a_lost_ball_costs_a_life_and_returns_to_the_paddle`, `a_ball_inside_the_field_costs_no_life`, `losing_the_last_life_ends_the_game`; 3) начисление очков — `destroying_a_brick_adds_one_hundred_points`, `a_second_hit_destroys_the_strong_brick_and_scores_two_hundred`, `an_indestructible_brick_never_breaks_and_never_scores`; 4) уничтожение `Normal` — `a_hit_destroys_the_brick`, `a_normal_brick_is_still_destroyed_by_one_hit_and_scores_one_hundred`; 5) повреждение и уничтожение `Strong` — `a_strong_brick_survives_the_first_hit`, `a_second_hit_destroys_the_strong_brick_and_scores_two_hundred`; 6) неразрушимость `Indestructible` — `an_indestructible_brick_never_breaks_and_never_scores`, `an_indestructible_brick_bounces_the_ball_like_any_other`; 7) отскок от ракетки — `hit_at_the_paddle_center_sends_the_ball_up`, `hit_left_of_the_paddle_center_sends_the_ball_left`, `hit_right_of_the_paddle_center_sends_the_ball_right`; 8) завершение уровня — `clearing_the_last_breakable_brick_completes_the_level`, `a_field_left_with_only_indestructible_bricks_is_complete`; 9) переход между уровнями — `advancing_loads_the_next_level_and_keeps_score_and_lives`; 10) `GameOver` — `losing_the_last_life_ends_the_game`, `game_over_freezes_the_frame`, `the_start_signal_does_not_revive_a_finished_game`; 11) `GameWon` — `clearing_the_last_level_wins_the_game`, `game_won_freezes_the_frame`; 12) полный reset — `a_full_restart_returns_the_game_to_its_start`. Пробелов нет, все двенадцать пунктов закрыты существующими тестами без дописывания нового покрытия по D-18. Перепроверка T-ARK-7/T-ARK-9: все восемь ранее позеленевших с первого запуска тестов (`waiting_to_start_keeps_the_ball_on_the_paddle`, `the_start_signal_does_not_revive_a_finished_game`, четыре теста бонуса из `paddle.rs`/`bonus.rs`, `every_fourth_destroyed_brick_drops_a_bonus`, `hitting_an_indestructible_brick_does_not_count_towards_a_bonus`, `a_setback_clears_the_bonuses_and_their_effect`) сверены дословно с текстом принятых решений (D-19, D-20, описание `T-ARK-9` в backlog) — каждый проверяет формулировку плана, а не побочный эффект кода; отдельно найдена и закрыта одна реальная двусмысленность — `Game::update` вызывает `paddle.update` безусловно, поэтому ракетка продолжает двигаться в `GameOver`/`GameWon`, хотя комментарий на той же функции говорит «кадр ничего не меняет», а существовавшие тесты `game_over_freezes_the_frame`/`game_won_freezes_the_frame` проверяли только мяч и счёт. Сверка с прецедентом Pong (`T-PONG-6`, `paddles_move_while_waiting_for_the_serve`, «ракетки управляются во всех состояниях») подтвердила, что это намеренное поведение, а не дефект; закрыто тестом `terminal_states_still_move_the_paddle`. RED получен изолирующей мутацией, так как код уже вёл себя верно: тест на первом прогоне дал `make test-target TEST=terminal_states_still_move_the_paddle` → exit 0, поэтому `paddle.update` в `Game::update` обёрнут условием, исключающим `GameOver`/`GameWon`, — мутация дала exit 2, `ракетка не сдвинулась в состоянии GameOver: x = 420`, при exit 0 контрольных `game_over_freezes_the_frame` и `game_won_freezes_the_frame`; мутация откачена, `git diff arkanoid/src/game.rs` не содержит удалённых строк — продакшн-код не изменился. Аудит простоты: `grep` по `trait `, `dyn `, `Box<dyn`, `impl Trait`, `<T`, `where `, `unwrap()`, `expect(`, `panic!`, `static mut`, `lazy_static`, `once_cell`, `.clone()`, `load_texture`, `load_sound`, `load_ttf`, `load_file`, `include_bytes` в `arkanoid/src/` — 0 совпадений; сущности ограничены `Game`, `Paddle`, `Ball`, `Brick`/`BrickKind`, `Bonus`, `Level` (модуль функций, не структура), `Score`, `GameState`, `Field` (геометрия поля, тот же паттерн, что и в Pong с `T-PONG-1`); между крейтами `pong` и `arkanoid` нет ни `use` друг друга, ни общего типа — `Field` продублирован в каждом крейте независимо (принятая цена D-02); grep по D-21 (physics, ecs, save, leaderboard, multiplayer, animation, boss, enemy) и по загрузке внешних assets — 0 совпадений; README не упоминает каркас работы с агентом (D-15). Проверка независимости от FPS на уровне логики подтверждена существующими тестами `paddle_moves_the_same_distance_regardless_of_frame_rate`, `ball_moves_the_same_distance_regardless_of_frame_rate`, `a_bonus_falls_the_same_distance_regardless_of_frame_rate` — новых тестов не потребовалось. GREEN: `make test-target TEST=terminal_states_still_move_the_paddle` → exit 0 (52 теста Arkanoid, 30 Pong). Точечно: `make test-target TEST=game` → exit 0 (19 из 52 в Arkanoid, 8 из 30 в Pong). Полный гейт: `make check` → exit 0 (D-06 целиком: `cargo fmt --check`, clippy `-D warnings`, `cargo test --workspace`, `cargo build --workspace`), перепрогнан после закрытия backlog-записи → exit 0. Специальный gate: владелец лично запустил `cargo run -p arkanoid`, играл больше двух минут, процесс завершился штатно (exit 0); ответ владельца дословно — «отлично. прошел первый уровень. увидел второй. он отличается. все ок». Подтверждено наблюдением владельца: запуск и вид поля, управление ракеткой, подача, разрушение блоков и счёт, завершение первого уровня, переход на второй уровень с другой раскладкой (закрывает пункт 9 чек-листа). Не наблюдалось владельцем и не записывается как отдельно подтверждённое: раскладка третьего уровня, состояние `GameWon`/экран `YOU WIN`, потеря жизни, `GAME OVER`, рестарт по `R`, бонус `Wide Paddle` — владелец подтвердил прогон общей фразой «все ок», но эти шаги отдельно не называл. Вариант принят «по тесту» в сокращённом виде: переход в `GameWon` на последнем уровне принят по unit-тесту `clearing_the_last_level_wins_the_game` (`T-ARK-8`) вместо наблюдения, третий уровень владелец не видел. Замечаний у владельца нет. |
| T-MENU-2 | DONE | Крейт `menu` реализован третьим участником workspace: `Selection` — чистая логика курсора по `ITEMS` (`PONG`/`ARKANOID`/`QUIT`) с заворотом в обе стороны, подтверждением и `escape()` (то же намерение, что `QUIT`, D-23); `main.rs` — `#[macroquad::main]`, три пункта меню отрисованы примитивами, выбранный подсвечен цветом, ловушка кадра возврата закрыта явным `next_frame().await` сразу после `pong::run().await`/`arkanoid::run().await` и до следующего чтения ввода; курсор живёт вне игрового цикла, поэтому позиция сохраняется после возврата, а повторный вход в игру начинает её заново без отдельного кода сброса (D-24). `default-members = ["menu"]` в корневом `Cargo.toml` даёт `cargo run` без `-p`; оба README игр и корневой README называют это как основной способ запуска, сохраняя `cargo run -p <игра>` для разработки. Ловушка кадра возврата unit-тестом не покрывается принципиально (D-10, ввод не тестируется) — единственное свидетельство её работоспособности это наблюдение владельца ниже. | RED по одному тесту за цикл, `make test-target TEST=<имя>` → exit 2: `move_down_moves_the_cursor_to_the_next_item` (курсор 0, ожидалось 1); `move_up_moves_the_cursor_to_the_previous_item` (курсор 0, ожидалось 1); `move_up_wraps_from_the_first_item_to_the_last` (курсор 0, ожидалось 2); `confirm_returns_the_item_under_the_cursor` (Pong вместо Arkanoid); `quit_confirms_to_the_intent_to_quit` (Pong вместо Quit). Три теста позеленели с первого запуска (стаб `move_down`/`move_up` — no-op, а `confirm`/`escape`/`label` — тривиально верные чистые функции с самого начала), RED получен изолирующими мутациями по одной за раз: `move_down_wraps_from_the_last_item_to_the_first` — `(cursor + 1) % ITEMS.len()` → `(cursor + 1).min(ITEMS.len() - 1)` дало `курсор с последнего пункта не завернул на первый: 2, ожидалось 0` при exit 0 контрольного `move_down_moves_the_cursor_to_the_next_item`; `escape_gives_the_same_intent_as_quit` — `escape()` → `self.confirm()` дало `Esc вернул Pong, ожидалось Quit` при exit 0 контрольного `quit_confirms_to_the_intent_to_quit`; `each_item_label_matches_its_name` — подписи `Pong`/`Arkanoid` переставлены местами, тест сам себе контроль (сверяет все три пункта), дал `left: "ARKANOID", right: "PONG"`. Все три мутации откачены, sha256 `menu/src/selection.rs` до и после совпал (`9af7629e…3e15235`), `git diff` по файлу (untracked, сверен вручную) не показывает следов мутаций. GREEN: `make test-target TEST=selection` → exit 0 (8 passed: 5 из ранее красных + 3 из позеленевших с первого запуска). Точечно: `cargo build --manifest-path Cargo.toml -p menu` → exit 0, `cargo build -p pong --bin pong` → exit 0, `cargo build -p arkanoid --bin arkanoid` → exit 0 (все три бинарника продолжают собираться самостоятельно). Полный гейт: `make check` → exit 0 (D-06 целиком по workspace: `cargo fmt --all -- --check`, `cargo clippy --workspace --all-targets --all-features -- -D warnings`, `cargo test --workspace` — 8 Menu + 30 Pong + 52 Arkanoid, `cargo build --workspace`). Аудит простоты: `grep` в `menu/src/` по `trait `, `dyn `, `Box<dyn`, `impl Trait`, `<T`, `where `, `unwrap()`, `expect(`, `panic!`, `static mut`, `lazy_static`, `once_cell`, `.clone()`, `load_texture`, `load_sound`, `load_ttf`, `load_file`, `include_bytes` — 0 совпадений. `default-members = ["menu"]` проверен фактическим прогоном: `cargo run --manifest-path Cargo.toml` (в фоне, снят через 3 секунды) → `Compiling menu v0.1.0`, `Running \`target/debug/menu\`` — способ сработал, README называет `cargo run` как основной запуск. **Специальный gate пройден и подтверждён владельцем**: прогон `cargo run` из корня выполнял лично владелец (не агент), длился больше двух минут — то есть меню и обе игры реально использовались, а не просто открылись; дословный ответ владельца — «все ок», замечаний нет. Это же наблюдение подтверждает и ловушку кадра возврата (единственное её свидетельство, unit-тестом не покрывается), и все 7 пунктов чек-листа `T-MENU-2` из бывшего `workflow/backlog.md`. |
| T-AST-1 | DONE | Репозиторий стал cargo-workspace из четырёх крейтов: новый крейт `asteroids` открывает окно с полем, `lib.rs` с единственным публичным элементом `pub async fn run()` (модулей внутри пока нет — им ещё нечего объявлять) и `Field`, объявленным в этом крейте (D-02, дублирование Pong/Arkanoid — принятая цена), стадии кадра D-09 видны как структура кода, `main.rs` — 16 строк по форме `pong/src/main.rs`, `Esc` выходит из цикла кадра. Pong, Arkanoid и menu не тронуты. | RED наблюдаемый, теста не потребовалось (по прецеденту `T-ARK-1`): `cargo test --manifest-path ./Cargo.toml -p asteroids` → exit 101, `error: package ID specification \`asteroids\` did not match any packages`. GREEN — та же команда → exit 0 после создания крейта. Unit-тестов нет и это не пробел: всё поведение позиции — окно и отрисовка, а они не тестируются по D-10. Точечно: `make test-target TEST=game` → exit 0 (8 Pong из 30). Полный гейт: `make check` → exit 0 (90 тестов без изменений: 52 Arkanoid + 8 menu + 30 Pong), `make boundary-check` → exit 0, `make context-check` → exit 0. Специальный gate: прогон `cargo run -p asteroids` выполнял владелец, а не агент; gate подтверждён владельцем, дословный ответ на вопрос про окно, тёмный фон со светлой рамкой поля и закрытие по `Esc` — «да все так», замечаний нет. |
| T-AST-2 | DONE | Корабль Asteroids существует как данные (`position: Vec2`, `angle: f32`) в приватном модуле `ship.rs`, поворачивается через delta time клавишами `A`/`←` (против часовой) и `D`/`→` (по часовой), `facing()` — единственная функция перехода «угол → направление» и единственная тригонометрия крейта (D-25), отрисован треугольником с видимым носом через `vertices()`, которая переиспользует `facing()` без повторного `sin`/`cos`. `velocity` и радиус столкновения по вписанной окружности (D-27) сознательно отложены до `T-AST-3` и `T-AST-8` — на этой позиции у них не было бы ни одного потребителя вне теста, а `cargo clippy --all-targets -- -D warnings` помечает такие поля как dead code. | RED по одному тесту за цикл на стабе (`facing()` → `Vec2::ZERO`, `rotate()` no-op), `cargo test -p asteroids` → exit 101: `angle_zero_faces_right` — «угол 0 дал направление Vec2(0.0, 0.0), ожидалось (1, 0)»; `a_quarter_turn_faces_down_the_screen` — «угол π/2 дал направление Vec2(0.0, 0.0), ожидалось (0, 1)»; `rotating_for_a_second_is_the_same_at_60_and_120_fps` — «60 кадров по 1/60: angle = 0, ожидалось π». Тест `holding_both_turn_keys_leaves_the_angle_unchanged` позеленел на стабе с первого запуска (no-op `rotate` тривиально не меняет угол), RED получен изолирующей мутацией `turn_direction`: `_ => 0.0` → `_ => 1.0` дало exit 101, «angle = 3.1415927, ожидалось 0», при exit 0 контрольного `angle_zero_faces_right`; мутация откачена, sha256 `asteroids/src/ship.rs` совпал (`b01482c9…`). GREEN: `cargo test -p asteroids` → exit 0 (4 passed). Точечно: `make test-target TEST=ship` → exit 0 (4 из 4). Полный гейт: `make check` → exit 0, 94 теста (52 Arkanoid + 4 Asteroids + 8 menu + 30 Pong). Специального gate на позиции нет. |
| T-MENU-1 | DONE | Каждая игра получила `pub async fn run()` в `lib.rs` (единственный публичный элемент, модули `ball`/`game`/`paddle`/`score` и аналогичные у Arkanoid остались приватными), `main.rs` похудел до `window_conf` + `#[macroquad::main] async fn main() { <игра>::run().await }`; обе игры продолжают собираться самостоятельными бинарниками, игровая логика не менялась. Формулировка `Esc` в обоих README приведена к D-23 (возврат в меню; при отдельном запуске возвращаться некуда, окно закрывается). | Перенос без изменения поведения, наблюдаемый RED недоступен по природе задачи — заменён сверкой неизменности: sha256 `pong/src/{ball,game,paddle,score}.rs` до и после совпали побайтно; `diff` `pong/src/lib.rs` против `git show pong-1.0:src/main.rs` дал ровно три ожидаемых различия (сигнатура `run`, изъятый `window_conf`, изъятый `#[macroquad::main]`), всё остальное — те же строки в том же порядке; тот же приём применён к `arkanoid/src/lib.rs` против прежнего `arkanoid/src/main.rs` (та же единственная замена). Список тестов `cargo test -p pong -- --list` и `cargo test -p arkanoid -- --list` до/после совпал именами: 30/30 у Pong, 52/52 у Arkanoid — тесты просто стали компилироваться как часть библиотечной цели, а не бинарника. Точечно: `make test-target TEST=game` → exit 0. Полный гейт: `make check` → exit 0 (D-06 целиком по workspace: `cargo fmt --all -- --check`, `cargo clippy --workspace --all-targets --all-features -- -D warnings`, `cargo test --workspace`, `cargo build --workspace`); дополнительно `cargo build -p pong --bin pong` и `cargo build -p arkanoid --bin arkanoid` → exit 0, подтверждая, что оба бинарника продолжают собираться самостоятельно. Специальный gate позиции («наблюдаемый прогон `cargo run -p pong` и `cargo run -p arkanoid`») по решению управляющего свёрнут в финальный прогон `T-MENU-2`, которая в любом случае открывает обе игры через меню и проверяет то же самое и сверх того; отдельно на этой позиции не запускался. **Подтверждено**: прогон `cargo run` владельцем на позиции `T-MENU-2` (дословный ответ «все ок») закрыл и этот свёрнутый gate — обе игры действительно открывались из меню в одном и том же окне. |
| T-AST-3 | DONE | Корабль Asteroids разгоняется вдоль `facing()` и летит по инерции: `velocity: Vec2` добавлено вместе с первым потребителем (`apply_thrust`/`advance`, перенос с `T-AST-2`), `velocity += facing() * ACCELERATION * delta_time` затем `position += velocity * delta_time` двумя отдельными строками, скорость никогда не присваивается напрямую, ограничение `clamp_length_max(MAX_SPEED)` действует каждый кадр (в том числе с `delta_time = 0.0` в кадрах без тяги — тот самый путь ловушки), ввод `W`/`↑`, факел двигателя рисуется только пока тяга включена. Торможение и трение не добавлены (запрещены D-31). | RED по одному тесту за цикл на стабе (`apply_thrust`/`advance` no-op), `cargo test -p asteroids` → exit 101: `thrust_along_the_x_axis_increases_velocity_x_only` — «тяга вдоль оси X дала velocity.x = 0, ожидалось 300»; `the_ship_keeps_moving_after_thrust_stops` — «корабль не пролетел по инерции: x = 480, ожидалось 630»; `coasting_covers_the_same_distance_regardless_of_frame_rate` — «60 кадров по 1/60: x = 480, ожидалось 680»; `clamping_the_speed_does_not_change_its_direction` — «ограничение не сработало: длина осталась 500». Два теста позеленели на стабе с первого запуска, оба закрыты изолирующими мутациями: `sustained_thrust_never_exceeds_the_speed_limit` — удалён `clamp_length_max`, дало exit 101 «скорость превысила предел: 1500.0001, ожидалось не больше 400» при exit 0 контрольного `thrust_along_the_x_axis_increases_velocity_x_only`; `a_still_ship_without_thrust_stays_put_and_finite` — `clamp_length_max` заменён на описанную планом ловушку (`normalize()` без проверки длины), дало exit 101 «координаты корабля стали нечисловыми: Vec2(NaN, NaN)» — воспроизведён именно тот дефект, который называет ловушка позиции — при exit 0 того же контрольного теста; обе мутации откачены, sha256 `asteroids/src/ship.rs` совпал (`6139c5bd…`). GREEN: `cargo test -p asteroids` → exit 0 (10 passed). Точечно: `make test-target TEST=ship` → exit 0 (10 из 10). Полный гейт: `make check` → exit 0, 100 тестов (52 Arkanoid + 10 Asteroids + 8 menu + 30 Pong). Специального gate на позиции нет. |
| T-AST-4 | DONE | Корабль Asteroids заворачивается по обеим осям через `rem_euclid`: объект существует ровно в одной точке (D-28), корректно при перелёте больше размера поля за один кадр; `Ship::wrap` вызывается в стадии обновления кадра после `advance`. | RED по одному циклу, `cargo test -p asteroids` → exit 101: `wrapping_past_the_left_edge_reappears_on_the_right` — «выход за левую границу дал x = -5, ожидалось 955»; `wrapping_past_the_right_edge_reappears_on_the_left` — «выход за правую границу дал x = 965, ожидалось 5»; `wrapping_past_the_top_edge_reappears_on_the_bottom` — «выход за верхнюю границу дал y = -5, ожидалось 595»; `wrapping_past_the_bottom_edge_reappears_on_the_top` — «выход за нижнюю границу дал y = 605, ожидалось 5»; `overshooting_by_several_field_widths_still_lands_inside_the_field` — «перелёт на три ширины поля дал x = -2860, ожидалось 20». Тест `a_ship_inside_the_field_does_not_move` позеленел на стабе с первого запуска, RED получен изолирующей мутацией `wrap`: `value.rem_euclid(size)` → `(value + 1.0).rem_euclid(size)` дало exit 101, «корабль внутри поля сместился» при exit 0 остальных пяти тестов; мутация откачена, `git diff --stat asteroids/src/ship.rs` после отката показал только исходные добавленные строки (108 insertions, 0 deletions) — продакшн-строка после отката идентична принятой. GREEN: `cargo test -p asteroids` → exit 0 (16 passed). Точечно: `make test-target TEST=wrap` → exit 0 (4 из 16 в Asteroids по подстроке `wrapping`). Полный гейт: `make check` → exit 0 с первого прогона, 106 тестов (52 Arkanoid + 16 Asteroids + 8 menu + 30 Pong). Специального gate на позиции нет. |
| T-AST-5 | DONE | Корабль Asteroids стреляет: `Bullet` (`position`, `velocity`, время жизни) и `Weapon` (cooldown) в новом модуле `bullet.rs`; пуля появляется у носа корабля (`Ship::nose_position`) со скоростью `ship.velocity + facing() * BULLET_SPEED`, летит по delta time, заворачивается тем же правилом, что корабль, и удаляется по истечении времени жизни; `Space` в `lib.rs` стреляет безусловно — различение по `GameState` (`WaitingToStart`/`PlayerDestroyed` не стреляют) откладывается до `T-AST-8`, где состояние впервые появляется. | RED по одному тесту за цикл на реализации с намеренно неверным `Weapon` (`shoot` всегда возвращает пулю, `tick` no-op — единственная сознательно стабированная часть, `Bullet`/`remove_expired`/`wrap` написаны сразу верно), `cargo test -p asteroids --lib -- bullet::` → exit 101: только `a_second_shot_in_the_same_frame_is_blocked_by_cooldown` упал по своей причине — «второй выстрел в том же кадре прошёл, ожидался блок по cooldown»; это сбой последовательности (поведение `Bullet` попало в код раньше своих тестов), зафиксирован как есть. Семь тестов позеленели на первом прогоне, каждый закрыт изолирующей мутацией по одной за раз при exit 0 контрольных тестов: `shooting_adds_one_bullet_aimed_where_the_ship_faces` — `velocity: ship.velocity + facing()*BULLET_SPEED` → `ship.velocity` дало «пуля полетела в направлении (0.0, 0.0)»; `the_bullet_spawns_ahead_of_the_ship_not_at_its_center` — `position: ship.nose_position()` → `ship.position` дало «пуля появилась в центре корабля»; `a_shot_after_the_cooldown_expires_goes_through` и `cooldown_ticks_the_same_at_60_and_120_fps` — общая мутация `cooldown > 0.0` → `cooldown >= 0.0` дала отказ в обоих сразу; `an_expired_bullet_is_removed_while_a_live_one_stays` — `retain(|b| !b.is_expired())` → `retain(|_| true)` дало «осталось 2, ожидалась 1»; `a_bullet_wraps_at_the_field_edge` — `x.rem_euclid(width)` → `x` дало «пуля за левой границей дала x = -5, ожидалось 955»; `a_bullet_outruns_a_ship_at_its_speed_limit` (переписан из первоначального сравнения констант — clippy `assertions_on_constants` отверг сравнение `BULLET_SPEED > 400.0` как результат, известный на этапе компиляции, поэтому тест разгоняет реальный корабль до предела скорости и сравнивает фактические длины векторов) — `BULLET_SPEED = 600.0` → `0.0` дало «скорость пули 0 не превысила скорость разогнанного корабля 400». Все мутации откачены; файл untracked (новый), поэтому идентичность после отката подтверждена построчным чтением файла и повторным `cargo test -p asteroids --lib -- bullet::` → exit 0 (8 passed) после каждого отката, а не `git diff`. GREEN: `cargo test -p asteroids --lib -- bullet::` → exit 0 (8 passed). Точечно: `make test-target TEST=bullet` → exit 0 (8 из 24 в Asteroids). Полный гейт: `make check` → exit 0 (первый прогон exit 2 на clippy: `assertions_on_constants`, тест переписан на разгон реального корабля вместо сравнения констант, второй прогон → exit 0), 114 тестов (52 Arkanoid + 24 Asteroids + 8 menu + 30 Pong). Специального gate на позиции нет. |
| T-AST-6 | DONE | Астероиды в новом модуле `asteroid.rs`: `Asteroid` (`position`, `velocity`, `size`), движение и заворачивание тем же правилом, что корабль/пуля, детерминированная раскладка первой волны кольцом вокруг центра поля с равным шагом угла и минимальным отступом от центра. `enum AsteroidSize` пока объявляет только `Large` — `Medium`/`Small` переехали в `T-AST-7`, их первый потребитель (тот же приём, что для `velocity` корабля на `T-AST-2`: вариант без потребителя вне теста — dead code под `cargo build --workspace -D warnings`, чего `cargo test` не ловит из-за `#[cfg(test)]`). | RED по одному тесту за цикл на стабе (`radius()` возвращает одно и то же число для всех размеров, `advance`/`wrap` no-op, `spawn_wave` возвращает пустой вектор), `cargo test -p asteroids --lib -- asteroid::` → exit 101: `large_is_bigger_than_medium_is_bigger_than_small` (тест ещё существовал на стабе, до переноса) — «Large (10) не больше Medium (10)»; `moving_covers_the_same_distance_regardless_of_frame_rate` — «60 кадров по 1/60: x = 100, ожидалось 220»; `wraps_at_the_field_edge` — «выход за левую границу дал x = -5, ожидалось 955»; `the_first_wave_has_the_expected_number_of_large_asteroids` — «первая волна дала 0 астероидов, ожидалось 4». Тест `no_asteroid_in_the_starting_wave_spawns_too_close_to_the_center` позеленел на стабе с первого запуска (пустой вектор не нарушает отступ вакуумно), RED получен изолирующей мутацией: `ring_radius = MIN_SPAWN_DISTANCE + 60.0` → `MIN_SPAWN_DISTANCE - 60.0` дало exit 101 при exit 0 остальных четырёх тестов; мутация откачена, `cargo test -p asteroids --lib -- asteroid::` → exit 0 (4 passed) подтвердил возврат к исходному поведению (файл untracked — сверка построчным чтением, как в `T-AST-5`). После реализации сборка `cargo build --workspace` (внутри `make check`) упала на `-D dead-code`: варианты `Medium`/`Small` нигде не строились вне `#[cfg(test)]`; вариант `AsteroidSize` сокращён до одного `Large`, тест на убывание радиуса по трём размерам и сам тест `large_is_bigger_than_medium_is_bigger_than_small` перенесены в `T-AST-7` (первый настоящий потребитель — разбиение), `wraps_at_the_field_edge` переписан на `AsteroidSize::Large`. GREEN: `cargo test -p asteroids --lib -- asteroid::` → exit 0 (4 passed). Точечно: `make test-target TEST=asteroid` → exit 0 (4 из 28 в Asteroids). Полный гейт: `make check` → exit 0 (первый прогон exit 2 на `cargo build --workspace`, `-D dead-code`, исправлено сокращением `AsteroidSize`; второй прогон → exit 0), 118 тестов (52 Arkanoid + 28 Asteroids + 8 menu + 30 Pong). Специального gate на позиции нет. |
| T-AST-7 | DONE | Пуля уничтожает астероид: `collision::circles_overlap` (круг-круг вручную, D-27, касание на сумме радиусов трактуется как столкновение); `AsteroidSize` получил `Medium`/`Small` вместе с первым потребителем — `Asteroid::split` (`Large` → два `Medium`, `Medium` → два `Small`, `Small` без осколков, отклонение на фиксированный угол в обе стороны, без RNG — D-30); `combat::resolve` в новом модуле `combat.rs` разрешает столкновения пуль и астероидов по ловушке позиции (локальный `Vec` осколков, пометка выбывших, `retain` + `append` после обхода, одна пуля — не больше одного астероида за кадр); `Score` в новом модуле `score.rs`, очки по размеру (`Large` 20, `Medium` 50, `Small` 100). Отображение счёта — `T-AST-10`. | RED по одному тесту за цикл на стабах (`circles_overlap` всегда `false`, `Asteroid::split` всегда пустой вектор, `Score::add` no-op, `AsteroidSize::radius/speed/points` возвращают одно и то же число для всех размеров, `combat::resolve` no-op), `cargo test -p asteroids --lib -- collision:: score:: combat::` → exit 101, 11 из 12 тестов новых модулей упали по своей причине: `circles_closer_than_the_radius_sum_overlap` — «круги на расстоянии 15 при сумме радиусов 20 не посчитаны столкнувшимися»; `circles_touching_exactly_at_the_radius_sum_overlap` — то же для расстояния 20; `a_hit_removes_the_bullet` — «попавшая пуля не исчезла»; `hitting_a_large_asteroid_removes_it_and_adds_two_medium` — «осталось 1 астероидов, ожидалось 2»; `hitting_a_medium_asteroid_removes_it_and_adds_two_small` и `hitting_a_small_asteroid_removes_it_and_adds_nothing` — аналогично; `the_two_fragments_fly_apart_in_different_directions` — паника по индексу (осколков не было); `score_is_credited_by_the_size_of_the_destroyed_asteroid` и `adding_points_increases_the_score` — счёт остался 0; `destroying_a_small_asteroid_scores_more_than_a_large_one` — «Small (20) не дороже Large (20)» (points() стаб одинаков для всех размеров); `one_bullet_destroys_exactly_one_asteroid_even_when_two_overlap` — «осталось 2 астероидов, ожидался 1». Тест `circles_farther_than_the_radius_sum_do_not_overlap` позеленел на стабе с первого запуска (стаб всегда `false` совпал с ожидаемым `false`), RED получен изолирующей мутацией: `(a_position - b_position).length() <= a_radius + b_radius` → `true` дало exit 101 при exit 0 остальных двух тестов `collision::`; мутация откачена, `cargo test -p asteroids --lib -- collision::` → exit 0 (3 passed) подтвердил возврат (файл untracked — сверка построчным чтением). GREEN: `cargo test -p asteroids --lib -- collision:: score:: combat::` → exit 0 (12 passed). После реализации `cargo build --workspace` (внутри `make check`) упал на `-D dead-code`: `Score::value` не имел потребителя вне теста (отображение счёта — `T-AST-10`), метод сокращён до `#[cfg(test)] pub(crate) fn value` — тот же приём, что для `velocity` корабля на `T-AST-2` и `AsteroidSize` на `T-AST-6`. Точечно: `make test-target TEST=combat` → exit 0 (8 из 40 в Asteroids). Полный гейт: `make check` → exit 0 (первый прогон exit 2 на `cargo build --workspace`, `-D dead-code` на `Score::value`, исправлено; второй прогон → exit 0), 130 тестов (52 Arkanoid + 40 Asteroids + 8 menu + 30 Pong). Специального gate на позиции нет. |
| T-AST-8 | DONE | Игра проходима и проигрываема: `enum GameState { WaitingToStart, Playing, PlayerDestroyed, GameOver }` (без `WaveCompleted` — нет потребителя до `T-AST-9`) и `Game` в новом модуле `game.rs` собрали поле, корабль, пули, астероиды и счёт; переходы целиком видны в `Game::update`. `Ship::collision_radius` — радиус по вписанной окружности треугольника корабля (D-27), той же тройкой точек, что `vertices()`. Столкновение с астероидом отнимает жизнь и переводит в `PlayerDestroyed`, respawn — по истечении таймера паузы (2с) автоматически, без клавиши; неуязвимость (2с) после respawn; ноль жизней — `GameOver`, в котором кадр не меняет ничего и `Space` не воскрешает игру; `Space` в `WaitingToStart` начинает раунд, в `Playing` стреляет (та же дифференциация, что `T-AST-5`, теперь через реальное состояние); `R` — полный рестарт через `Game::restart` (пересоздание конструктором). `lib.rs::run()` переписан на использование `Game` вместо разрозненных локальных переменных. Поле `wave` в `Game` не заведено — первый читающий потребитель появится в `T-AST-9`. | RED по одному тесту за цикл на стабе (`Ship::collision_radius` возвращает 9999.0, `Game::update` no-op), `cargo test -p asteroids --lib -- ship::tests::the_collision_radius... game::` → exit 101, 6 из 9 новых тестов упали по своей причине: радиус корабля — «радиус столкновения 9999 больше вписанного радиуса …»; `a_collision_with_an_asteroid_costs_a_life_and_destroys_the_player` — «жизней после столкновения — 3, ожидалось 2»; `invulnerability_expires_and_the_next_collision_costs_a_life` — аналогично; `losing_the_last_life_ends_the_game` — «жизней осталось 1, ожидалось 0»; `the_respawn_pause_counts_down_by_delta_time_and_resets_the_ship` — «по истечении паузы состояние — PlayerDestroyed, ожидалось Playing»; `the_start_signal_begins_the_round_from_waiting_to_start` — «сигнал старта не начал раунд: состояние WaitingToStart». Три теста позеленели на стабе с первого запуска: `an_invulnerable_ship_does_not_lose_a_life_to_the_same_collision` и `game_over_freezes_the_frame_and_ignores_the_start_signal` (стаб-no-op тривиально ничего не менял, что совпало с ожидаемым «не изменилось») и `a_full_restart_returns_everything_to_the_start` (`restart()` был реализован сразу верно, не стабирован). Все три закрыты изолирующими мутациями при exit 0 контрольных тестов: удаление проверки `invulnerability_timer > 0.0` в `check_ship_collision` дало «жизней после столкновения — 2, ожидалось 3» (неуязвимый корабль потерял жизнь); подмена условия заморозки `GameState::GameOver` на `GameState::WaitingToStart` в начале `update` дала «корабль сдвинулся в GameOver»; подмена тела `restart()` на пустой блок дала «жизни после рестарта — 0, ожидалось 3». Все мутации откачены; файлы untracked/новые — сверка построчным чтением и повторным `cargo test -p asteroids --lib -- game::` → exit 0 (8 passed) после каждого отката. GREEN: `cargo test -p asteroids --lib -- ship:: game::` → exit 0 (17 + 8 passed). После реализации `cargo build --workspace` упал на `-D dead-code`: поле `wave` нигде не читалось вне теста (первый читающий потребитель — `T-AST-9`), поле убрано из `Game` и из теста рестарта; решение записано в `T-AST-9` в backlog. Точечно: `make test-target TEST=game` → exit 0 (8 из 49 в Asteroids). Полный гейт: `make check` → exit 0 (первый прогон exit 2 на `cargo fmt --check`, исправлено `make format`; второй прогон exit 2 на `cargo build --workspace`, `-D dead-code` на `wave`, исправлено; третий прогон → exit 0), 139 тестов (52 Arkanoid + 49 Asteroids + 8 menu + 30 Pong). Специального gate на позиции нет. |
| T-AST-9 | DONE | Уничтожение последнего астероида переводит `Playing` в `GameState::WaveCompleted` (новый вариант, вводится вместе с первым потребителем) и снимает пули прошлой волны; `Space` в `WaveCompleted` продвигает волну (`Game::advance_wave`) — та же дифференциация одной клавиши по состоянию, что в `T-AST-5`/`T-AST-8`; `Game::wave` (отложено с `T-AST-8`) растёт на единицу; `asteroid::spawn_wave` считает число крупных астероидов как `START_ASTEROIDS + N - 1` с потолком `MAX_WAVE_ASTEROIDS = 12`; счёт и жизни не трогаются переходом волны, потому что это поля матча, а не волны. | RED по одному тесту за цикл: рост числа астероидов по волне и переход `Playing → WaveCompleted` через `Space` — на исходном коде до правок (`cargo test -p asteroids --lib -- asteroid::tests::a_later_wave... game::`) → exit 101: `a_later_wave_has_more_large_asteroids_than_the_first` — «волна 3 дала 4 астероидов, волна 1 — 4, ожидался рост» (до введения формулы роста); после добавления `GameState::WaveCompleted` со стаб-веткой (`Space` ничего не делает, коллекция не переводит игру в `WaveCompleted`) — `destroying_the_last_asteroid_completes_the_wave` — «опустевшая коллекция не завершила волну: состояние Playing»; `the_next_wave_increases_the_wave_number` — «номер волны после перехода — 1, ожидалось 2»; `the_next_wave_has_more_large_asteroids_than_the_previous_one` — «следующая волна дала 4 астероидов, предыдущая — 4»; `advancing_to_the_next_wave_keeps_the_score_and_lives` упал по состоянию (осталось `WaveCompleted` вместо `Playing`). Два теста позеленели на стабе с первого запуска: `wave_asteroid_count_never_exceeds_the_cap` (константное число 4 тривиально не превышало потолок 12) и `the_wave_does_not_end_while_an_asteroid_remains` (стаб никогда не завершал волну, что совпало с «волна не должна завершиться»). Оба закрыты изолирующими мутациями при exit 0 контрольных тестов: снятие `.min(MAX_WAVE_ASTEROIDS)` из формулы количества дало «волна 50 дала 53 астероидов, потолок 12»; замена условия `self.state == GameState::Playing && self.asteroids.is_empty()` на `self.state == GameState::Playing` дала «волна завершилась при оставшемся астероиде: состояние WaveCompleted». Обе мутации откачены; `asteroid.rs`/`game.rs` untracked — сверка построчным чтением и повторным прогоном соответствующего теста → exit 0. GREEN: `cargo test -p asteroids --lib -- asteroid:: game::` → exit 0 (6 + 13 passed). Точечно: `make test-target TEST=wave` → exit 0 (9 из 56 в Asteroids). Полный гейт: `make check` → exit 0 с первого прогона, 146 тестов (52 Arkanoid + 56 Asteroids + 8 menu + 30 Pong). Специального gate на позиции нет. |
| T-AST-10 | DONE | Игрок видит счёт, жизни, номер волны и понимает, чего ждёт игра: `draw_status` рисует строку `Score`/`Lives`/`Wave` из `Game`; `GameState::message(self, wave)` — чистая функция текста по состоянию (`WaitingToStart` → `Press SPACE to start`, `Playing` → нет сообщения, `PlayerDestroyed` → `Ship destroyed`, `WaveCompleted` → `Wave {следующий номер}`, `GameOver` → `GAME OVER\nPress R to restart`), `draw_message` центрирует его по полю; `combat::resolve` теперь возвращает позиции уничтоженных астероидов, `Game::effects` — короткоживущие `Effect{position, time_remaining}` с растущим затухающим кругом при отрисовке (`Effect::progress`); `Score::value` стал полноценно публичным — первый потребитель вне теста. | RED по одному тесту за цикл на стабах (`GameState::message` всегда `None`, `combat::resolve` возвращал пустой вектор при сохранённой старой логике попаданий), `cargo test -p asteroids --lib -- game::tests::each_state... combat::tests::a_hit_reports... game::tests::destroying_an_asteroid_spawns... game::tests::an_effect_disappears...` → exit 101: `each_state_shows_its_own_message` — «assertion failed: left: None, right: Some("Press SPACE to start")»; `a_hit_reports_the_destroyed_asteroids_position` — «уничтоженные позиции — [], ожидалось [(100, 100)]»; `destroying_an_asteroid_spawns_an_effect_at_its_position` — «эффектов после уничтожения — 0, ожидался 1»; `an_effect_disappears_after_its_duration` — тот же сбой (эффект не появился). Тест `a_frame_without_a_hit_reports_no_destroyed_asteroids` позеленел на стабе с первого запуска (пустой вектор совпал с ожидаемым «нет уничтоженных»), RED получен изолирующей мутацией: безусловное добавление позиции первого оставшегося астероида в возвращаемый вектор после `append(&mut debris)` дало exit 101, «кадр без попаданий сообщил уничтоженные позиции: [(100, 100)]», при exit 0 контрольного `a_hit_reports_the_destroyed_asteroids_position`; мутация откачена, `cargo test -p asteroids --lib -- combat::` → exit 0 (10 passed) подтвердил возврат (файлы untracked — сверка построчным чтением). GREEN: `cargo test -p asteroids --lib -- game:: combat::` → exit 0 (16 + 10 passed). Точечно: `make test-target TEST=message` → exit 0 (1 из 61 в Asteroids). Полный гейт: `make check` → exit 0 (первый прогон exit 2 на `cargo fmt --check`, исправлено `make format`; второй прогон → exit 0), 151 тест (52 Arkanoid + 61 Asteroids + 8 menu + 30 Pong). Размер: 814 продакшн-строк (без тестов) — в пределах ожидаемых 900–1100 для всей игры, порог тревоги ~1300 не достигнут. Специального gate на позиции нет. |
| T-AST-11 | DONE | Asteroids попал в меню: `menu/Cargo.toml` получил зависимость `asteroids`, `MenuItem::Asteroids` — четвёртый вариант (`ITEMS: [MenuItem; 4]`, подпись `"ASTEROIDS"`), `menu/src/main.rs` — ветка `MenuItem::Asteroids => { asteroids::run().await; next_frame().await; continue; }` с той же ловушкой кадра возврата (D-23), что у `pong`/`arkanoid`; порядок пунктов `PONG`, `ARKANOID`, `ASTEROIDS`, `QUIT`; корневой `README.md` — `Game 03 — Asteroids` получил ссылку в каталог `asteroids/` (README игры появится в `T-AST-12`, поэтому ссылка на директорию, не на файл) и статус «в разработке», в списке запусков добавлен `cargo run -p asteroids`. | RED — ожидаемое падение существующего теста меню, предсказанное backlog: `cargo test -p menu` (через workspace-манифест) → exit 101, `quit_confirms_to_the_intent_to_quit` — «подтверждение на пункте QUIT вернуло Asteroids, ожидалось Quit» (два `move_down()` при четырёх пунктах указывают на `Asteroids`, а не на `Quit`); исправлено третьим `move_down()` — это приведение теста к изменившемуся списку, а не ослабление проверки. Два новых теста (`each_item_label_matches_its_name` с добавленной строкой на `Asteroids`, `moving_down_two_items_points_at_asteroids`) позеленели с первого запуска, так как правки enum/`ITEMS`/`label()` уже были внесены раньше самих тестов; RED получен изолирующими мутациями при exit 0 контрольных тестов: `MenuItem::Asteroids => "ASTEROIDS"` → `"WRONG"` дало «assertion failed» в `each_item_label_matches_its_name`; третий элемент `ITEMS` подменён с `MenuItem::Asteroids` на `MenuItem::Quit` (дубль) дало «подтверждение после двух move_down вернуло Quit, ожидалось Asteroids» в `moving_down_two_items_points_at_asteroids`; обе мутации откачены, `cargo test -p menu` → exit 0 (9 passed) после каждого отката (файлы отслеживаемые — сверка `git diff` пустая после отката). GREEN: `cargo test -p menu` (через workspace-манифест) → exit 0 (9 passed). Полный гейт: `make check` → exit 0 (первый прогон exit 2 на `make boundary-check` — карточка задачи содержала абсолютные пути хоста в командах проверки, что хук трактует как выход за границу репозитория; команды переписаны без абсолютного пути; второй прогон → exit 0), 152 теста (52 Arkanoid + 61 Asteroids + 9 menu + 30 Pong). **Специальный gate (наблюдаемый прогон `cargo run`: меню → Asteroids → меню, `Esc` не закрывает программу) пропущен по решению владельца 2026-09-25** (дословно: «пропускай проверку на экране. проверим сразу когда вся ветка закончится»); прогон отложен до завершения всей очереди Asteroids. Отложенный прогон выполнен владельцем после слияния в `main` (`45b510d`): 2026-09-25, дословно — «все работает». |
| T-AST-12 | DONE | `asteroids/README.md` объясняет игру программисту, впервые изучающему game development: запуск (через меню и `cargo run -p asteroids`), таблица управления с объяснением, почему одна клавиша `Space` значит разное по состоянию, game loop и его стадии, `Vec2`, ускорение/скорость/инерция с ловушкой нулевого вектора при `clamp_length_max`, углы и `facing()` как единственный переход «угол → направление», screen wrapping через `rem_euclid`, время жизни объектов, коллекции пуль и астероидов, collision detection (круг-круг, ловушка добавления осколков во время обхода), разбиение астероидов и почему мелкий дороже крупного, состояния игры и respawn-по-таймеру, волны и раскладка первой волны, раздел «чем отличается от Pong и Arkanoid», таблица файлов. При сверке найден и исправлен один расходившийся с кодом комментарий: doc-комментарий `Ship::facing()` утверждал, что через неё выражается «разлёт осколков» астероида, хотя `Asteroid::split()` берёт направление из `velocity.normalize()`, а не из `facing()` (у астероида нет угла); комментарий и соответствующая фраза README исправлены. | RED/GREEN неприменимы: задача не добавляет поведения, набор тестов не изменился (152 теста). Проверка — сверка каждого утверждения README с исходниками: семь блоков кода сверены дословно (`ship.rs` — `apply_thrust`, `advance`, `facing`; `bullet.rs` — `advance`/`is_expired`, `wrap`; `asteroid.rs`/`combat.rs` — `wrap`, добавление осколков в сокращении с пометкой; `collision.rs` — `circles_overlap`); один блок (`wrap`) изначально процитирован по тексту `bullet.rs`/`asteroid.rs`, но подписан как общий для всех трёх типов — уточнено явным комментарием в блоке, что `ship.rs` реализует то же самое через свою локальную функцию `wrap(value, size)`. Таблица управления сверена с привязками клавиш в `lib.rs` (`A`/`Left`, `D`/`Right`, `W`/`Up` через `is_key_down`; `Space`, `R`, `Escape` через `is_key_pressed`). Текст пяти сообщений состояний — с `GameState::message`; строка статуса — с `draw_status`; три жизни — с `LIVES`; формула роста волны — с `spawn_wave`; очки 20/50/100 — с `AsteroidSize::points`. Упоминаний каркаса работы с агентом нет (D-15): `grep -in "workflow\|CLAUDE\|agent\|агент\|backlog" asteroids/README.md` — без совпадений (exit 1). Хвостовые пробелы: `grep -c " $" asteroids/README.md` → 0. Полный гейт: `make check` → exit 0 с первого прогона, 152 теста без изменений (52 Arkanoid + 61 Asteroids + 9 menu + 30 Pong) — подтверждает, что задача не изменила поведение. Специального gate у позиции нет. |
| T-AST-13 | DONE | Аудит покрытия D-26 закрыл все 14 пунктов поимённо, нашёл и закрыл один пробел (тест на убывание радиуса `Large > Medium > Small`, обещанный в backlog при переносе с `T-AST-6` на `T-AST-7`, но не дописанный). Аудит простоты нашёл и исправил лишнюю сущность: `Weapon` (cooldown выстрела) убран, cooldown стал простым полем `Game::shoot_cooldown: f32`, `Weapon::tick`/`shoot` заменены свободными функциями `bullet::tick_cooldown`/`bullet::shoot` — рефакторинг без изменения поведения (D-25: «таймеры — простые поля в `Game`, а не отдельные сущности»). Найдены и зафиксированы как принятое отклонение (не исправлялось кодом — правка ушла бы за пределы задачи и в чужую зону `roadmap.md`) два расхождения с буквальным текстом D-25: `Effect` — сущность визуального эффекта, введённая в `T-AST-10` для требования задания «visual feedback», которого не было на момент написания D-26.08.14; минимальна (позиция + один таймер), архитектурно идентична `Bullet`, что и позволяет считать её приемлемым продолжением уже принятого паттерна, а не нарушением духа решения. Второе: `AsteroidSize::split()` и раскладка волны (`spawn_wave`) используют `Vec2::from_angle`/`.cos()`/`.sin()` в двух местах помимо `Ship::facing()` — текст D-25 предполагал, что «разлёт осколков... выражается через `facing()`», но реализация `T-AST-7` берёт направление осколков из вектора скорости родителя-астероида (у которого нет угла и нет доступа к кораблю), а раскладка волны (`T-AST-6`) требует разместить переменное число точек по кольцу, что принципиально нуждается в тригонометрии независимо от `facing()`; текст решения D-25 в `roadmap.md` устарел относительно принятой реализации — это вне зоны записи `developer` (см. `Roles` в `CLAUDE.md`), поэтому исправление roadmap.md не сделано, находка адресована в отчёте. Остальной аудит простоты — без замечаний: нет `use pong`/`use arkanoid`/общего модуля, нет запрещённых конструкций D-02/D-07 (`unwrap()`/`expect()` только в тестах, без `clone()`, `static mut`, `trait`/`dyn`/generics, warnings), нет пунктов D-31, внешних assets нет. Размер: 816 продакшн-строк (без тестов), в пределах ожидаемых 900–1100, порог тревоги ~1300 не достигнут. Пункты чек-листа 1–18 и 20 подтверждены тестами/кодом (перечень в отчёте задачи); пункт 19 закрыт `T-AST-12`. | RED по одному тесту: `large_is_bigger_than_medium_is_bigger_than_small` в `asteroid.rs` — продакшн-код уже был верным (радиусы 40/24/12 заданы с `T-AST-7`), поэтому тест позеленел на первом прогоне (`cargo test -p asteroids --lib -- asteroid::tests::large_is_bigger...` → exit 0), RED получен изолирующей мутацией: `AsteroidSize::Medium => 24.0` → `50.0` дало exit 101, «Large (40) не больше Medium (50)», при exit 0 остальных шести тестов `asteroid::`; мутация откачена, `git diff asteroids/src/asteroid.rs` после отката не тронул `pub fn radius` — только добавленный тест (19 insertions, 0 deletions). Удаление `Weapon` — рефакторинг без нового поведения: `cargo test -p asteroids --lib -- bullet:: game::` → exit 0 (24 passed) до и после замены `Weapon` на `shoot_cooldown: f32` + свободные функции, поэтому в список RED не включается. GREEN: `cargo test -p asteroids --lib -- asteroid:: bullet:: game::` → exit 0. Полный гейт: `make check` → exit 0 (первый прогон exit 2 на `cargo fmt --check`, исправлено `make format`; второй прогон → exit 0), 153 теста (52 Arkanoid + 62 Asteroids + 9 menu + 30 Pong). **Специальный gate (наблюдаемый прогон по чек-листу приёмки первой версии, 11 шагов) пропущен по решению владельца 2026-09-25** (дословно: «пропускай проверку на экране. проверим сразу когда вся ветка закончится»); шаги, требующие наблюдения владельцем: запуск игры; вращение корабля; ускорение и инерция; пересечение всех границ поля; уничтожение астероида каждого размера и переход `Large → Medium → Small`; потеря жизни и respawn; завершение минимум одной волны; `GameOver`; полный restart; запуск через `cargo run` из меню и возврат по `Esc` без закрытия программы. Все шаги, кроме последнего (вход в меню), покрыты unit-тестами и подтверждены через них в этой сессии; чек-лист приёмки первой версии (20 пунктов) верен по коду и тестам, наблюдаемое подтверждение владельца остаётся открытым. Отложенный прогон выполнен владельцем после слияния в `main` (`45b510d`): 2026-09-25, дословно — «все работает». |
